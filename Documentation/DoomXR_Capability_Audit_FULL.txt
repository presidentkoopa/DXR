====================================================================================================
DOOMXR ENGINE CAPABILITY AUDIT — FULL RAW RESULTS (all 45 claims adversarially challenged)
Final tally: {'working': 13, 'partial': 17, 'declared_unwired': 11, 'absent': 4} | Adversary overturned: 9 | Claims: 45
====================================================================================================

####################################################################################################
CLAIM ID: grip.1  |  CLUSTER: Grip, Climbing & Gravity Gloves
CLAIM: Climbing on keyworded textures with modder-friendly keywording

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
Fully wired end-to-end: the canonical per-tic P_PlayerThink calls VR_UpdateClimbing, which resolves climbability against linedef/sector Keywords and the wall texture name via the KeywordDispatcher (initialized at startup from KEYWORDS.json, which has concrete climb entries), then moves the player by inverted hand velocity and clears gravity while gripping. Modder-friendly keywording is real on two fronts: UDMF 'keywords'/'user_climbable' linedef fields and the texture-name convention 'climb:'+texName driven by KEYWORDS.json. Minor defect I found: user_climbable=true (udmf.cpp:1205) appends the BARE token 'climbable' but IsClimbable only matches profiles keyed 'climb:NAME', so that specific shortcut token never matches — however the explicit keywords= path and the texture-name path both work. Confirmed p_user.cpp (not p_user_backup.cpp) is the compiled file via src/CMakeLists.txt:966.

--- INVESTIGATOR EVIDENCE ---
  1. src/playsim/p_user.cpp:1435 — P_PlayerThink (called from p_tick.cpp:172) invokes VR_UpdateClimbing(player) every tic on the live player-think path
  2. src/playsim/p_user.cpp:1893-1909 — VR_UpdateClimbing calls KeywordDispatcher::IsClimbable on line->Keywords, on 'climb:'+texture-name, and on front/back sector Keywords, then applies hand-velocity motion + MF_NOGRAVITY (p_user.cpp:1937-1971)
  3. src/playsim/keyword_dispatcher.cpp:90-104,212-225 — Init() parses the 'climb' namespace into climbable profiles and IsClimbable consumes them (Init() called at src/d_main.cpp:3656)
  4. wadsrc/static/KEYWORDS.json:47-60 — real climb entries (LADDER, PIPE1-6, CLIMBABLE, TEKWALL, METAL...) with climb_speed the texture-name lookup consumes
  5. src/maploader/udmf.cpp:1197,1200-1206 — UDMF linedef parser reads 'keywords' and 'user_climbable' into ld->Keywords, giving mappers a keyword surface

ADVERSARIAL: survives=True finalVerdict=working (conf high)
--- REFUTATION ---
The original verdict survives. I could not find any missing link: the function is on the live per-tic path, is not a stub, is not gated off by default cvars, its keyword consumer is initialized at startup and reads shipped JSON data (confirmed baked into doomxr.pk3), and mappers get two real keywording surfaces. The only flaw is a cosmetic one-alias mismatch (user_climbable appends bare 'climbable' which never matches the 'climb:CLIMBABLE' profile key), which does not degrade the working feature. Verdict: working / high.
 --- ADVERSARY EVIDENCE ---
  1. src/playsim/p_user.cpp:1435 — VR_UpdateClimbing(player) called unconditionally in P_PlayerThink, the per-tic player-think path (live, not a comment)
  2. src/playsim/p_user.cpp:1893,1899,1905,1909,1969-1970 — IsClimbable consumed against line/sector Keywords and 'climb:'+texName, then MF_NOGRAVITY set and player->mo->Vel driven by inverted hand velocity (real logic, not a stub)
  3. src/playsim/keyword_dispatcher.cpp:89-104,212-225 + src/d_main.cpp:3656 — Init() parses climb namespace into profiles['climb:NAME'] and IsClimbable reads them; Init called at engine startup
  4. src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:492-493 — vr_climb_radius=32.0 and vr_climb_speed_mult=1.0 defaults are positive, so the feature is NOT gated off by default
  5. src/maploader/udmf.cpp:1197,1758 + src/gamedata/r_defs.h:1557,734 — UDMF loader populates line_t.Keywords and sector_t.Keywords from mapper 'keywords=' fields (real modder surface)

####################################################################################################
CLAIM ID: grip.3  |  CLUSTER: Grip, Climbing & Gravity Gloves
CLAIM: Unlocked grip bindings with context-based use of grip for climbing, catching, and throwing

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
Grip bindings are truly rebindable and wired end-to-end from the OpenVR grip button through the input map to the playsim button state read by VR_IsGripPressed, and are surfaced as Control rows in menudef. Context-based use is explicit and real: P_PlayerThink runs a documented priority (Climb > Gravity Gloves > Two-Hand), climbing suppresses the glove grip, the glove path implements a lock/pull/catch/throw state machine, throw velocity is mass-scaled and gated by the throwable keyword, and the resulting per-hand vr_hand_state drives the VRHandModel to swap Grip/Climb animation states in the model renderer. One incomplete detail I found: the menu exposes vr_grab_weight_dist/align/mass sliders (menudef.txt:3353-3355) that NO C++ reads — the candidate scoring at p_user.cpp:1585 uses a hardcoded dot*(1-dist/max) formula — but this only affects candidate-ranking tuning, not the climb/catch/throw context switching, which is fully functional.

--- INVESTIGATOR EVIDENCE ---
  1. src/rendering/gl/stereo3d/gl_openvr.cpp:2285-2294 -> src/g_game.cpp:851-852 -> src/d_event.h:78-79 — OpenVR k_EButton_Grip maps to KEY_VR_LGRIP/RGRIP -> BT_VR_LGRIP/RGRIP in ucmd.buttons, read by VR_IsGripPressed (p_user.cpp:1335-1342); binds in commonbinds.txt:16-17 and menudef.txt:707-708
  2. src/playsim/p_user.cpp:1434-1437 — explicit context priority in P_PlayerThink: 'Climb > Gravity Gloves > Two-Handing' calling VR_UpdateClimbing, VR_UpdateGravityGloves, VR_CalculateTwoHanding in order
  3. src/playsim/p_user.cpp:1470-1473,1498-1524,1741-1797 — grip context state machine: climbing overrides grip; on release throws throwable/held item with mass-scaled velocity; lock/pull/waiting-catch/catch states for the pull-and-catch flow
  4. src/playsim/p_user.cpp:1439-1449 -> src/r_data/models.cpp:222-231 — vr_hand_state (Idle/Grip/Climb) set per context and consumed by the hand-model renderer to swap VRHandModel to the Grip/Climb ZScript state (class in wadsrc/static/zscript/actors/shared/vr_hand.zs:16-24)
  5. src/playsim/keyword_dispatcher.cpp:275-287 -> src/playsim/p_user.cpp:1504,1685 — IsThrowable consumes the flags:throwable keyword (KEYWORDS.json:73-74) to gate throw behavior

ADVERSARIAL: survives=True finalVerdict=working (conf high)
--- REFUTATION ---
I re-investigated independently and tried hard to break the "working" verdict; it survives. Attack 1 — is the context logic on a live path? P_PlayerThink is the sole live per-tic entry: p_tick.cpp:172 calls it inside the MAXPLAYERS loop, and CMakeLists.txt:966 compiles playsim/p_user.cpp while the stale playsim/p_user_backup.cpp (which has its own duplicate P_PlayerThink) is NOT in the build — so the grip logic that compiles is the live one. Attack 2 — is the grip input actually rebindable and read? OpenVR maps k_EButton_Grip to KEY_VR_LGRIP/RGRIP (gl_openvr.cpp:2285-2294; OpenXR mirrors it at vk_openxrdevice.cpp:3402); action names vr_lgrip/vr_rgrip are registered (d_main.cpp:3032-3033) to Button_VR_LGrip/RGrip (d_buttons.h:41-42), which g_game.cpp:851-852 folds into BT_VR_LGRIP/RGRIP, and VR_IsGripPressed reads those ucmd.buttons bits (p_user.cpp:1335-1342). Rebindable Control rows exist in menudef.txt:707-708. One evidence discrepancy: their cited commonbinds.txt does NOT exist at wadsrc/static/commonbinds.txt (my grep/sed found no such file), so the default-bind citation is wrong — but this does not weaken the claim, since the menudef Control rows plus registered action names prove the binds are truly rebindable. Attack 3 — is the context state machine real or stubbed? It is fully real: P_PlayerThink runs VR_UpdateClimbing then VR_UpdateGravityGloves then VR_CalculateTwoHanding (p_user.cpp:1435-1437) in the documented Climb>Gloves>TwoHand priority; climbing sets isGripPressed=false to suppress the glove path (1470-1473); the glove path implements lock->pull->waiting-catch->catch->held (1529-1558, 1743-1754), bullet snatch (1761-1782), magnet pull (1718-1722, 1791-1797), mass-scaled throw (1509-1513), drop, and auto-equip (1729-1735) — every branch mutates player fields re-read next tic. Attack 4 — keyword/cvar consumers? IsThrowable/IsClimbable are real branching consumers (keyword_dispatcher.cpp:212-287) of the throwable/climbable profile flags in KEYWORDS.json:73-74; all driving CVars are declared with sane non-zero defaults and actually read by acting logic (hw_vrmodes.cpp:486-492, p_actionfunctions.cpp:92-95). Attack 5 — hand-model consumption? vr_hand_state (set per context at p_user.cpp:1441-1448) is consumed by models.cpp:222-231 to FindState("Grip"/"Climb") on VRHandModel, whose Grip/Climb states exist in vr_hand.zs:16-20. The only genuine gap (the vr_grab_weight_dist/align/mass menu sliders that no C++ reads, and the hardcoded dot*(1-dist/max) score at 1585) is tuning-only and does not affect climb/catch/throw switching. Verdict stands.
 --- ADVERSARY EVIDENCE ---
  1. src/p_tick.cpp:172 + src/CMakeLists.txt:966 — P_PlayerThink is the live per-tic caller and p_user.cpp is the compiled unit (p_user_backup.cpp with its duplicate P_PlayerThink is NOT in the build), so the grip context logic actually runs
  2. src/g_game.cpp:851-852 + src/d_main.cpp:3032-3033 + src/playsim/p_user.cpp:1335-1342 + wadsrc/static/menudef.txt:707-708 — OpenVR grip -> registered vr_lgrip/vr_rgrip action -> BT_VR_LGRIP/RGRIP -> VR_IsGripPressed reads the bit; binds are rebindable Control rows (their commonbinds.txt citation is wrong: that file does not exist at wadsrc/static/, but menudef proves rebindability)
  3. src/playsim/p_user.cpp:1435-1448 — P_PlayerThink runs Climb>GravityGloves>TwoHand in order then derives vr_hand_state per hand from the resulting climb/held/lock/pull/stabilized state
  4. src/playsim/p_user.cpp:1498-1524,1743-1797 — real lock/pull/waiting-catch/catch/snatch/magnet/mass-scaled-throw state machine; each branch mutates player->vr_* fields re-read next tic (not a stub)
  5. src/r_data/models.cpp:222-231 + wadsrc/static/zscript/actors/shared/vr_hand.zs:16-20 + src/playsim/keyword_dispatcher.cpp:212-287 — vr_hand_state swaps VRHandModel Grip/Climb states, and IsThrowable/IsClimbable are real consumers of KEYWORDS.json flags

####################################################################################################
CLAIM ID: grip.4  |  CLUSTER: Grip, Climbing & Gravity Gloves
CLAIM: Bullet and projectile grabbing (catch incoming projectiles)

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
Projectile grabbing is wired on a live path: the gravity-glove candidate scan admits MF_MISSILE actors, the cone/scoring selects the best one, and on grip-press with vr_allow_bullet_snatching enabled the missile is frozen (Vel zeroed + NOGRAVITY), re-parented to the player, and given catch haptics/sparks; because held missiles are treated as throwable they can be thrown back. All gating CVars are defined with real defaults and menu-exposed. The only caveat I found: a second, richer snatch-OR-deflect implementation using a swept hand-volume is present but fully commented out (p_user.cpp:1799-1852), and the only 'deflect' code in the file lives inside that dead block — so the active feature is catch/snatch-and-throw-back, not mid-air deflection. That does not undermine the core claim of catching incoming projectiles, which is functional.

--- INVESTIGATOR EVIDENCE ---
  1. src/playsim/p_user.cpp:1573 — candidate scan explicitly accepts MF_MISSILE actors ('if (!(mo->flags & MF_SPECIAL) && !(mo->flags & MF_MISSILE)) continue;'), so incoming projectiles become grab candidates via the cone selector
  2. src/playsim/p_user.cpp:1761-1782 — live snatch path: if vr_allow_bullet_snatching && (cand->flags & MF_MISSILE) && distToHand < vr_catch_radius, missile Vel zeroed, MF_NOGRAVITY set, target reassigned to player, with catch haptic and spark particles
  3. src/playsim/p_actionfunctions.cpp:92,95,98-99 — vr_allow_bullet_snatching (default true), vr_catch_radius, vr_catch_haptic, vr_catch_spark CVARs defined; menu-exposed menudef.txt:3379-3387
  4. src/playsim/p_user.cpp:1503,1684 — a snatched projectile (MF_MISSILE) is flagged throwable, so it feeds the throw path and can be hurled back with mass-scaled velocity on grip release
  5. src/playsim/p_user.cpp:1799-1852 — an ALTERNATIVE swept-volume snatch+DEFLECT implementation is entirely commented out ('/* Bullet Snatching logic (Swept Volume) - Tabled for now'), so that deflect-back behavior is dead code

ADVERSARIAL: survives=True finalVerdict=working (conf high)
--- REFUTATION ---
I attacked the "working" verdict by trying to break every link in the chain, and all held. (a) Candidate admission: p_user.cpp:1573 is a real branch (not a comment) that lets MF_MISSILE actors through the cone selector. (b) Snatch path: p_user.cpp:1761-1782 is live uncommented code that zeroes missile Vel, sets MF_NOGRAVITY, reparents target to player, and fires catch haptic/spark. (c) CVar gating is real: vr_allow_bullet_snatching/vr_catch_radius/vr_catch_haptic/vr_catch_spark are defined in p_actionfunctions.cpp:92-99 with defaults true/24.0/true/true, EXTERN'd (p_user.cpp:116-119), read by the branch, and menu-exposed (menudef.txt:3379-3387) — none default-off. (d) The enclosing function VR_UpdateGravityGloves is called at p_user.cpp:1436 inside P_PlayerThink (1347), invoked per-tick from P_Ticker at 2207 — a genuinely live path. I checked the two early-return gates: VRMode-cached and vr_grab_max_dist<=0; the latter's default is 500.0 (hw_vrmodes.cpp:489), so it does not fire. (e) I confirmed only p_user.cpp is compiled (CMakeLists.txt:966); p_user_backup.cpp is NOT listed and .cpp sources are not GLOB'd, so the traced code is the real build. Snatched missiles are flagged throwable (1503/1684) and thrown back with mass-scaled velocity on release. The only caveat — the richer swept-volume snatch/DEFLECT block at 1799-1852 is fully commented-out dead code — is correct and does not affect the catch claim. Original verdict is upheld.
 --- ADVERSARY EVIDENCE ---
  1. src/playsim/p_user.cpp:1573 — real (non-comment) branch 'if (!(mo->flags & MF_SPECIAL) && !(mo->flags & MF_MISSILE)) continue;' admits incoming projectiles into the cone-scored grab-candidate selector
  2. src/playsim/p_user.cpp:1761-1782 — live snatch: vr_allow_bullet_snatching && (cand->flags & MF_MISSILE) && distToHand < vr_catch_radius -> missile Vel zeroed, MF_NOGRAVITY set, target=player, catch haptic + 16 spark particles
  3. src/playsim/p_user.cpp:1436 — VR_UpdateGravityGloves(player) called every tick inside P_PlayerThink (1347), reached from P_Ticker at 2207; not dead code
  4. src/playsim/p_actionfunctions.cpp:92-99 — vr_allow_bullet_snatching(true)/vr_catch_radius(24.0)/vr_catch_haptic(true)/vr_catch_spark(true) CVARs defined with non-off defaults, consumed by the snatch branch and exposed at menudef.txt:3379-3387
  5. src/CMakeLists.txt:966 — only playsim/p_user.cpp is compiled (p_user_backup.cpp is excluded, .cpp not GLOB'd), and vr_grab_max_dist defaults to 500.0 (hw_vrmodes.cpp:489) so the <=0 early-return gate never fires

####################################################################################################
CLAIM ID: grip.2  |  CLUSTER: Grip, Climbing & Gravity Gloves
CLAIM: High-fidelity gravity gloves with a visible debug cone, voxel toggle when in cone, mass and reactive physics

INVESTIGATOR VERDICT: partial (conf high)

--- INVESTIGATOR REASONING ---
Three of four sub-claims are genuinely wired: the debug cone is a real detailed particle visualization (beams + rings + candidate pulse) gated behind vr_grab_debug and menu-exposed; mass is real (mass keyword -> ResolveKeywordMass -> AActor::Mass at PostBeginPlay -> throwForce = vr_throw_force*(100/itemMass)); reactive physics (hand-magnet pull, LOS-drop, gravity-accurate trajectory arc, auto-equip) is fully implemented on the live path. What I looked for and could NOT find: any 'voxel toggle when in cone' — zero voxel code in VR_UpdateGravityGloves or the candidate scan; the only voxel code is the generic engine voxel renderer and a cosmetic VoxelParticle death effect, neither tied to grab candidacy. One advertised sub-feature is entirely absent while the rest is fully wired, so partial.

--- INVESTIGATOR EVIDENCE ---
  1. src/playsim/p_user.cpp:1436,1461 — VR_UpdateGravityGloves(player) is called from P_PlayerThink and fully implemented
  2. src/playsim/p_user.cpp:1595-1660 — debug cone/sphere/target-pulse rendered via P_SpawnParticle (beams, concentric rings), gated behind vr_grab_debug; cone params from vr_grab_cone_angle/vr_grab_max_dist (CVARs hw_vrmodes.cpp:488-489, menu-exposed menudef.txt:3346)
  3. src/playsim/p_mobj.cpp:5210-5213 — AActor::PostBeginPlay calls ResolveKeywordMass and sets Mass; consumed as itemMass in throw scaling (p_user.cpp:1509-1513 massScale=100/itemMass)
  4. src/playsim/p_user.cpp:1667-1736 — reactive physics: magnet-pull toward hand (vr_grab_magnet_speed), P_CheckSight LOS drop, projectile trajectory-arc sim with gravity, auto-equip on contact
  5. src/playsim/p_user.cpp:1561-1593 — no 'voxel' anywhere in the grab path; only generic engine RF_VOXELSPRITE rendering and an unrelated VoxelParticle death-effect actor exist, neither tied to grab

ADVERSARIAL: survives=True finalVerdict=partial (conf high)
--- REFUTATION ---
CONFIRMED partial (hardened). 3/4 live: debug cone is real (P_SpawnParticle beams+rings) but gated behind vr_grab_debug which DEFAULTS FALSE (hidden out-of-box); mass genuinely scales throw velocity (massScale=100/itemMass -> Vel); reactive physics (magnet-pull, P_CheckSight LOS-drop, gravity-arc, auto-catch) all live. Gloves ON by default (only gate vr_grab_max_dist=500). 'Voxel toggle when in cone' = ABSENT: no sprite<->voxel/RF_VOXELSPRITE swap tied to grab candidacy anywhere; the sole voxel code (vr_voxel_assembly.zs) is a weapon-equip flourish, not cone-tied.
 --- ADVERSARY EVIDENCE ---
  1. p_user.cpp:1436,1464 — VR_UpdateGravityGloves live per-tic, gloves on by default (vr_grab_max_dist=500)
  2. p_user.cpp:1595-1635 — debug cone real but gated by vr_grab_debug (default false, hw_vrmodes.cpp:389)
  3. vr_voxel_assembly.zs — only voxel code is A_DataSiphonEquip weapon ready-up flourish, NOT grab-cone-tied -> voxel-toggle absent

####################################################################################################
CLAIM ID: keyword.2  |  CLUSTER: Keyword System & Modder JSON/HTML Tools
CLAIM: Modder-editable keyword database included

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
A modder-editable JSON database exists, is loaded through the standard filesystem lump lookup (so pk3 overrides work), and is actually shipped in the build pk3. At least one namespace is consumed meaningfully at runtime: the climb block gates which wall textures are VR-climbable via IsClimbable('climb:'+textureName). The mass path also works, though via an inline 'mass:N' string parse (keyword_dispatcher.cpp:233) that technically bypasses the JSON mass block. Caveats I found but that do not sink the claim: several JSON namespaces have weak/no real consumer — the color namespace (neon_cyan/neon_pink) has no shipping actor tagged with 'color:*' (only the outline code path exists), and the loader ignores the anatomy/role/trait/vulnerability/ballistics blocks entirely. So the database is real, editable, shipped, and partially consumed — enough to call the claim itself (a modder-editable keyword DB is included and read) WORKING.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/KEYWORDS.json:1-88 — a real editable DB with theme + namespaces (anatomy, role, trait, vulnerability, climb, mass, color, flags, ballistics, weapons).
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:21 — Init() loads it via fileSystem.CheckNumForFullName("KEYWORDS.json", true), so a modder pk3 that overrides KEYWORDS.json is picked up by the normal Doom filesystem override rules.
  3. unzip -l E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3 shows KEYWORDS.json (3425 bytes, 2026-07-02) — the DB is actually shipped inside the engine pk3, not just in source.
  4. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1899 — IsClimbable("climb:"+tex->GetName()) resolves climbable wall textures against the JSON 'climb' block (SUPPORT2/LADDER/TEKWALL etc. at KEYWORDS.json:47-61), a genuine JSON->gameplay consumption path.
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/shared/vr_testing.zs:9,28 — actors reference JSON namespaces (climb:CLIMBABLE, flags:throwable) that the loader parses (keyword_dispatcher.cpp:90-104,157-169).

ADVERSARIAL: survives=True finalVerdict=working (conf high)
--- REFUTATION ---
The verdict survives: the modder-editable keyword DB is real, loaded at startup, overridable via filesystem lump rules, shipped in the pk3, and consumed by multiple live gameplay paths.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/d_main.cpp:3656 — KeywordDispatcher::Init() is called on the normal startup path (after ParseCVarInfo, ungated), proving the JSON is actually loaded at boot, not dead code
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:21 — Init() loads via fileSystem.CheckNumForFullName("KEYWORDS.json", true), so a modder pk3 overriding the lump wins by standard filesystem precedence = genuinely editable
  3. E:/DoomXR-work/DOOM_FRESH/src/maploader/udmf.cpp:1197,1758 — line_t/sector_t.Keywords are populated from UDMF map data, so the climb consumer at p_user.cpp:1893 reads real tags, not empty strings
  4. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_mobj.cpp:5211 — ResolveKeywordMass(Keywords) applied in AActor::PostBeginPlay (Mass = kw_mass), a live JSON->gameplay consumer beyond climb
  5. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:KEYWORDS.json — unzip -l shows KEYWORDS.json (3425 bytes, 2026-07-02) with real content shipped inside the engine pk3

####################################################################################################
CLAIM ID: keyword.1  |  CLUSTER: Keyword System & Modder JSON/HTML Tools
CLAIM: A robust keyword system that divorces Doom from traditional gameplay (game-from-gameplay), modder-forward

INVESTIGATOR VERDICT: partial (conf high)

--- INVESTIGATOR REASONING ---
The core dispatcher is genuinely wired end-to-end: it is compiled (CMakeLists:957), initialized on the live d_main startup path, reads KEYWORDS.json from the filesystem, and is consumed by real gameplay code — actor mass/VR-grab-weight in PostBeginPlay (p_mobj.cpp:5211), VR climbing and two-handing (p_user.cpp), throwables (p_user.cpp:1504/1685), and sprite outline color (hw_sprites.cpp:314). The 'Keywords' actor property is real and modder-settable, and shipping actors carry gameplay-divorcing tags. I verdict PARTIAL not WORKING because significant parts are decorative/broken: the C++ JSON loader only parses namespaces color/climb/mass/actor/pickup/flags/weapons, but the actor tags that ship (role/trait/anatomy/vulnerability/ballistics) are largely NOT parsed by the loader; the parry branch in hw_vrmodes.cpp:1206 queries the profile map with a bare class name while the loader stores 'weapons:'-prefixed keys and never parses parry_extent, so parry never resolves via JSON. The robust/divorcing skeleton runs, but a large fraction of the designed keyword surface has no consumer.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/d_main.cpp:3656 — KeywordDispatcher::Init() called on the live startup path (after ParseCVarInfo); keyword_dispatcher.cpp is in CMakeLists.txt:957 so it is compiled into the engine.
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_mobj.cpp:5211 — in AActor::PostBeginPlay() (runs for every spawned actor) ResolveKeywordMass reads actor->Keywords and writes Mass=kw_mass: a real gameplay effect (VR grab weight) driven by keywords.
  3. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1314 + :1893 — GetWeaponOffsets(ReadyWeapon->Keywords) drives VR two-handing and IsClimbable(line->Keywords) drives VR climbing; p_user.cpp is built (CMakeLists.txt:966) while p_user_backup.cpp is NOT in CMakeLists.
  4. E:/DoomXR-work/DOOM_FRESH/src/scripting/thingdef_properties.cpp:611 + actor.h:1682 — 'Keywords' is a genuine modder-facing ZScript actor property (FString Keywords); shipping actors set rich tags, e.g. cacodemon.zs:25 sets role:floater trait:projectile anatomy:flesh vulnerability:eye_crit.
  5. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:1206 — parry path calls GetProfile(weap TypeName)='Shotgun' but profiles are keyed 'weapons:Shotgun' (keyword_dispatcher.cpp:175), and parry_extent/recoil_force are never parsed from JSON (cpp:172-187): a broken branch.

ADVERSARIAL: survives=True finalVerdict=partial (conf high)
--- REFUTATION ---
I re-traced every layer independently and the PARTIAL verdict survives — the broken fraction is actually LARGER than the original auditor reported. WHAT WORKS end-to-end: (1) mass — ResolveKeywordMass special-cases the literal `mass:` token prefix (keyword_dispatcher.cpp:233-240, independent of JSON) and PostBeginPlay writes Mass for every spawned actor (p_mobj.cpp:5210-5213); shipping actors carry `mass:400`/`mass:40` so this is a genuine keyword-driven gameplay effect. (2) flags grenade/throwable — parsed (loader :157-169), 2 zscript files carry the tokens, consumed in p_user throwables. (3) climb — parsed (:90-104), consumed in p_user climbing with a texture-name fallback. Init() is on the live startup path (d_main.cpp:3656, right after ParseCVarInfo) and the property is a real modder-facing ZScript actor property (thingdef_properties.cpp:608, actor.h:1682); p_user_backup.cpp is NOT compiled (0 CMake refs). WHAT IS BROKEN/DECORATIVE (more than they claimed): (a) Two-handing offsets — GetWeaponOffsets matches only `weapons:`-prefixed tokens, but ZERO shipping weapons carry a `weapons:` token (all use `class:shotgun` etc.); confirmed 0 occurrences across the whole pk3 → offset lookup always returns false, falls back to default radius. The original auditor missed this. (b) Parry — double-broken: GetProfile queries the bare class name "Shotgun" while the loader stores "weapons:Shotgun" (key mismatch), AND parry_extent_* is never parsed from JSON so parry_extent_z defaults 0 → `parry_extent_z <= 0` continue always fires (hw_vrmodes.cpp:1207). (c) Ballistics/bullet_drop — the `ballistics` JSON namespace is never parsed (0 refs in loader) and ResolveMetadata never even copies bullet_drop (keyword_dispatcher.cpp:260-268), so p_mobj.cpp:2647 consumer is dead. (d) The entire semantic tag layer that embodies the 'divorce gameplay' thesis — role/trait/anatomy/vulnerability — is UNPARSED: the loader parses an "actor" namespace (:123) that does not exist in the shipped KEYWORDS.json, while the JSON's actual role/trait/anatomy/vulnerability namespaces have no loader branch, so tokens like `role:floater`, `anatomy:flesh`, `vulnerability:eye_crit` resolve to nothing. (e) color/outline — 0 actors carry `color:neon` tokens, so hw_sprites.cpp:314 has no live input. Conclusion: a real skeleton runs (mass/flags/climb wired end-to-end), but the majority of the designed, gameplay-divorcing keyword surface — semantic monster tags, weapon offsets, parry, ballistics, outlines — has no working consumer. PARTIAL, leaning to the low end.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:233-246 — ResolveKeywordMass parses the literal `mass:` token prefix (JSON-independent); cacodemon.zs:25 carries `mass:400`, consumed for every actor at p_mobj.cpp:5210-5213 (PostBeginPlay writes Mass) — a genuinely working keyword→gameplay effect.
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:172-187 — the weapons loader stores keys as `weapons:`+name and parses ONLY twohand_offset_*/twohand_radius; parry_extent and recoil_force are never read from JSON, so KeywordProfile.parry_extent_z stays 0 (header :31).
  3. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:1206-1207 — parry calls GetProfile(bare 'Shotgun') but keys are 'weapons:Shotgun', and `profile->parry_extent_z <= 0` is always true (never parsed) → the parry branch always `continue`s: doubly dead.
  4. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3!zscript/actors/doom/weaponshotgun.zs:18 — shipping weapon carries `class:shotgun` (and NO `weapons:` token; 0 `weapons:` tokens exist anywhere in the pk3), so GetWeaponOffsets (p_user.cpp:1314) never matches and two-hand offsets silently fall back to default radius.
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:123-138 — loader parses an 'actor' namespace absent from the shipped KEYWORDS.json, while the JSON's role/trait/anatomy/vulnerability/ballistics namespaces have NO loader branch (0 'ballistics'/'anatomy'/'vulnerability' parse refs), so cacodemon.zs:25 tags role:floater/anatomy:flesh/vulnerability:eye_crit resolve to nothing — the 'game-from-gameplay' tag layer is unconsumed.

####################################################################################################
CLAIM ID: keyword.3  |  CLUSTER: Keyword System & Modder JSON/HTML Tools
CLAIM: Extensive modder-forward tools for engine-level tweaks via external JSONs and HTMLs

INVESTIGATOR VERDICT: partial (conf high)

--- INVESTIGATOR REASONING ---
There is exactly one working external-config path: KEYWORDS.json, which the C++ engine actually loads. Everything on the HTML-tool side is unwired vaporware. Two HTML authoring tools exist (sdf_combo_authoring.html, sdf_upgrade_authoring.html) and they DO produce/round-trip JSON in-browser, but their own hint text ('the engine evaluates them live', 'auto-assembles them in-game') is exactly the prose the audit distrusts: the combo arbiter hardcodes its list in OnRegister and never reads sdf_combos.json, and sdf_upgrades.json has neither a file nor any consumer. So the 'extensive tools ... via external JSONs and HTMLs' claim is only fractionally real — the JSON half has one live consumer; the HTML half is a disconnected front-end whose exports nothing reads. Verdict PARTIAL: some real wiring (KEYWORDS.json) plus a stubbed/disconnected HTML tooling layer.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/tools/sdf_authoring/sdf_combo_authoring.html:41,91,137 — an HTML tool that exports sdf_combos.json and whose hint claims 'Put this file in wadsrc/static to load it in-engine' / 'the engine evaluates them live'.
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/ui/vr_sdf_combos.zs:19-46 — the ZScript arbiter HARDCODES its combos via AddCombo(...) in OnRegister() and contains no file/JSON read; grep of that file for .json/ReadLump/OpenFile returns nothing.
  3. grep of E:/DoomXR-work/DOOM_FRESH/src and wadsrc for 'sdf_combos.json' — zero consumers; the only 'sdf_combos' hit is the ZScript filename include (zscript.txt:21), NOT the JSON. So the HTML tool's output file is never loaded.
  4. E:/DoomXR-work/DOOM_FRESH/tools/sdf_authoring/sdf_upgrade_authoring.html:37,112,154 — exports sdf_upgrades.json claiming 'the engine auto-assembles them in-game', but no sdf_upgrades.json file exists and no ZScript/C++ upgrade-registry consumer was found.
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/KEYWORDS.json + keyword_dispatcher.cpp:21 — the ONE genuinely wired external-config path (JSON read by the engine); no HTML editor for KEYWORDS.json exists in the repo.

ADVERSARIAL: survives=True finalVerdict=partial (conf high)
--- REFUTATION ---
Partial survives. HTML half broken: combos hardcoded, no reader for sdf_combos.json or sdf_upgrades. But a second wired JSON path doomxr_weapons.json was missed. JSON half real, HTML half disconnected.
 --- ADVERSARY EVIDENCE ---
  1. vr_sdf_combos.zs:16 combos hardcoded via AddCombo no JSON read
  2. grep sdf_combos.json zero consumers
  3. keyword_dispatcher.cpp:21 reads KEYWORDS.json used by hw_sprites.cpp:314
  4. vr_weapon.cpp:69 reads doomxr_weapons.json via d_main.cpp:3770
  5. sdf_combo_authoring.html:91 hint but no engine consumer

####################################################################################################
CLAIM ID: keyword.4  |  CLUSTER: Keyword System & Modder JSON/HTML Tools
CLAIM: Triple-tag intersection resolver (Actor tags + Action tags + Style tags -> most-specific-match reaction)

INVESTIGATOR VERDICT: declared_unwired (conf high)

--- INVESTIGATOR REASONING ---
The resolver logic is fully written and compiles (zscript.txt:21 includes vr_sdf_combos.zs): SDFComboArbiter intersects actor tags (victim.GetTag + hit zone + hit hand), action tags (the '+'-joined trig conditions with AND matching), and style/class tags (c.klass weapon-class filter), which is exactly the claimed triple-tag intersection. But it is dead on arrival: it is a StaticEventHandler and is NOT listed in the MAPINFO AddEventHandlers block (common.txt:8, confirmed identical in the shipped pk3), and no C++/ZScript code registers it. Without registration its WorldThingDied override never fires, so no kill is ever evaluated and its only consumer (SpawnSigil) never runs. Additionally the matcher relies on victim.GetTag() rather than the actor's Keywords string, so even if registered it would not read the KEYWORDS the rest of the system uses. I looked for any registration (AddEventHandlers entry, RegisterStaticHandler call, GameInfo hook) and found none. Verdict declared_unwired: the resolver exists and is complete-looking but has zero live invocation path.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/ui/vr_sdf_combos.zs:62-116 — SDFComboArbiter.WorldThingDied builds fullContext=victim.GetTag()+lastHitZone+lastHitHand (actor tags), matches c.trig with '+' AND-intersection (action tags, e.g. 'dmg:energy + anatomy:cybernetic'), and filters by c.klass=weapon class (style tags): a genuine triple-tag intersection resolver.
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/mapinfo/common.txt:8 — AddEventHandlers lists GITD_DeathFXHandler, HF_GlowHandler, DarkDoomZ_Handler, GITD_FlashHandler, GITD_PresetHandler, GITD_BloomHandler, GITD_MuzzleHandler, gy_EventHandler, gy_VmAbortHandler, VRBlackoutHandler, GITD_JunkerScanHandler — SDFComboArbiter is ABSENT.
  3. unzip -p doomxr.pk3 mapinfo/common.txt — the shipped pk3 AddEventHandlers line is identical and also omits SDFComboArbiter, confirming it is not registered in the build.
  4. grep of E:/DoomXR-work/DOOM_FRESH/src for 'SDFComboArbiter'/'RegisterStaticHandler' referencing it — zero code-side registration; a StaticEventHandler that is neither in AddEventHandlers nor code-registered never receives WorldThingDied.
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/ui/vr_sdf_procedural.zs:51 + vr_sdf_combos.zs:114 — the resolver's only downstream, VRSigilManager.SpawnSigil, is called solely from inside the never-firing arbiter, so it too is dead.

ADVERSARIAL: survives=True finalVerdict=declared_unwired (conf high)
--- REFUTATION ---
I independently opened every relevant file and attacked the "declared_unwired" verdict by hunting for any registration the auditor might have missed. The resolver logic itself is genuine and matches the claim: E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/ui/vr_sdf_combos.zs:73 builds fullContext from victim.GetTag()+lastHitZone+lastHitHand (actor tags), lines 89-104 split c.trig on " + " and require ALL sub-conditions to be found in fullContext (action-tag AND-intersection, e.g. "dmg:energy + anatomy:cybernetic"), and line 107 filters by c.klass against inflictor.GetClassName() (style/weapon-class tag) — a real triple-tag intersection resolver. But it is dead on arrival. In GZDoom a StaticEventHandler subclass only receives WorldThingDied if registered via MAPINFO AddEventHandlers or an explicit EventHandler.RegisterNew/Register call, and I confirmed all paths are absent: (1) common.txt:8 AddEventHandlers lists 11 handlers and SDFComboArbiter is not one — I verified the shipped build's pk3 mapinfo/common.txt is byte-identical and also omits it; (2) the only other zmapinfo.txt/mapinfo.txt files in the tree are hacx/chex/urbanbrawl filter content and register nothing relevant, and the GITD repo (E:/_gitd35_repo) contains no mapinfo at all; (3) a full grep found zero RegisterNew/.Register(/EventHandler.Find calls targeting it in ZScript; (4) grep of src/ for SDFComboArbiter/vr_sdf_combos returned nothing — no C++ auto-registration. The only references outside the class's own body are a code COMMENT at vr_locational_damage.zs:55 (untrustworthy prose) and its own SpawnSigil call. Its sole downstream, VRSigilManager.SpawnSigil (vr_sdf_procedural.zs:51), is invoked only from inside the never-firing arbiter (vr_sdf_combos.zs:114), so it is dead too. The resolver is complete-looking but has zero live invocation path. Verdict declared_unwired confirmed.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/ui/vr_sdf_combos.zs:73,89-107 — WorldThingDied builds fullContext from victim.GetTag()+lastHitZone+lastHitHand (actor tags), AND-intersects '+'-split c.trig conditions (action tags), and filters by c.klass vs inflictor.GetClassName() (style tags): a genuine triple-tag intersection resolver
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/mapinfo/common.txt:8 — AddEventHandlers lists 11 handlers; SDFComboArbiter is absent, so its StaticEventHandler WorldThingDied override is never registered/fired
  3. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3 mapinfo/common.txt — shipped AddEventHandlers line is byte-identical to source and also omits SDFComboArbiter, confirming the build never registers it
  4. E:/DoomXR-work/DOOM_FRESH grep for SDFComboArbiter/RegisterNew/.Register — only hits are the class definition (vr_sdf_combos.zs:12), its own SpawnSigil call (:114), and a comment at vr_locational_damage.zs:55; zero registration in ZScript and zero references in src/
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/ui/vr_sdf_procedural.zs:51 + vr_sdf_combos.zs:114 — the resolver's only downstream, VRSigilManager.SpawnSigil, is called solely from inside the never-registered arbiter, so it is also dead code

####################################################################################################
CLAIM ID: locdmg.2  |  CLUSTER: Locational Damage
CLAIM: Damage multipliers per hit location

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
The head and leg multipliers are fully wired end-to-end in the live C++ path: CVars are declared with defaults (hw_vrmodes.cpp:395-396), externed into p_interaction.cpp (81-82), and actually consumed by the multiply at lines 1671/1679 which mutates the 'damage' int passed on to DamageMobj at line 1705 — so the reduced/boosted damage genuinely reaches the victim. They are also user-tunable via menu sliders. The only caveat (which keeps me from over-claiming) is that torso has no dedicated multiplier — it is the untouched default of 1.0 — but the claim is 'multipliers per hit location' and head+leg multipliers are real and applied on a live path, so this is working. Note the ZScript path's mult read (vr_locational_damage.zs:30-31) resolves to these same C++ CVars but is unreachable because that whole handler is broken by the invalid HitLocation access (see locdmg.1).

--- INVESTIGATOR EVIDENCE ---
  1. src/playsim/p_interaction.cpp:1671 — headshot applies damage = int(damage * vr_locational_head_mult) on the live damage path before DamageMobj is called
  2. src/playsim/p_interaction.cpp:1679 — legshot applies damage = int(damage * vr_locational_leg_mult)
  3. src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:395-396 — vr_locational_head_mult (default 2.0) and vr_locational_leg_mult (default 0.5) are real CVARs, ARCHIVE|GLOBALCONFIG
  4. src/playsim/p_interaction.cpp:81-82 — both mults are EXTERN_CVAR'd into p_interaction.cpp where the branching logic reads and multiplies with them
  5. wadsrc/static/menudef.txt:3395-3396 — sliders 'Headshot Mult' and 'Legshot Mult' expose these CVars to the user

ADVERSARIAL: survives=True finalVerdict=working (conf high)
--- REFUTATION ---
Original verdict survives: the head and leg location multipliers are fully wired on a live C++ damage path that every actor-damage call funnels through, the gate cvar defaults ON, and the shipped pk3 matches source.
 --- ADVERSARY EVIDENCE ---
  1. src/playsim/p_interaction.cpp:1671 — inside live DoDamageMobj, damage = int(damage * vr_locational_head_mult) mutates damage before it is passed to DamageMobj() at line 1705
  2. src/playsim/p_interaction.cpp:1679 — legshot branch damage = int(damage * vr_locational_leg_mult) on the same live path; bands (head top-20%, leg bottom-25%) are non-overlapping and coherent
  3. src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:392,395,396 — gate vr_locational_damage defaults true (NOT off), head_mult 2.0f, leg_mult 0.5f, all ARCHIVE|GLOBALCONFIG
  4. src/playsim/p_interaction.cpp:1733 & 1749 — DoDamageMobj (which holds the locational block at 1630) is called from BOTH the native DamageMobj VM binding and P_DamageMobj's else branch, so it is on every actor-damage path
  5. src/playsim/p_interaction.cpp:81-82 & menudef.txt:3395-3396 — both mults EXTERN_CVAR'd into the branching logic and exposed as user sliders; baked doomxr.pk3 actor.zs:804 native virtual DamageMobj confirms shipped build matches source

####################################################################################################
CLAIM ID: locdmg.1  |  CLUSTER: Locational Damage
CLAIM: Locational damage supporting headshots, legshots, torso shots via dynamic bounding + mathematical cylinder height

INVESTIGATOR VERDICT: partial (conf high)

--- INVESTIGATOR REASONING ---
Two parallel implementations exist and they disagree. The LIVE one is C++ in DoDamageMobj (p_interaction.cpp:1661), which every damage event flows through; it genuinely distinguishes head vs leg zones using a mathematical bounding cylinder derived from target->Height, gated by the default-on vr_locational_damage CVar. This is real, wired logic. However it is 'partial' relative to the full claim: (a) zone detection keys off inflictor->Z() (puff/projectile height), a fixed-percentage cylinder, not a true per-shot hit point; (b) 'torso' is not an explicit case, just the untouched default; (c) the ZScript VRLocationalArbiter that claims true 'dynamic bounding via HitLocation' is DEAD — it accesses e.Thing.HitLocation, a field that exists only on struct FLineTraceData, never on Actor (I searched src/ for any DEFINE_FIELD binding HitLocation to AActor and found none — only the FLineTraceData binding at vmthunks_actors.cpp:2258), so that access is invalid. The capability partially works via C++, so verdict is partial, not working.

--- INVESTIGATOR EVIDENCE ---
  1. src/playsim/p_interaction.cpp:1661 — LIVE C++ locational block sits inside DoDamageMobj (the universal damage path), gated by 'vr_locational_damage', ISMONSTER/player, non-explosion, non-melee; this runs on every qualifying hit
  2. src/playsim/p_interaction.cpp:1663-1665 — head/leg zones computed from a bounding cylinder off target->Height (headLower=floorz+Height-Height*0.20, headUpper=floorz+Height, legUpper=floorz+Height*0.25); zone chosen by inflictor->Z() vs these bounds at lines 1669/1677
  3. wadsrc/static/zscript/engine/vr_locational_damage.zs:24 — the parallel ZScript VRLocationalArbiter reads e.Thing.HitLocation.Z, but e.Thing is an Actor and Actor has NO HitLocation field
  4. wadsrc/static/zscript/actors/actor.zs:54 — 'native Vector3 HitLocation' is a member of struct FLineTraceData (struct opens actor.zs:36, closes :62), NOT of class Actor (which only starts at :77); no DEFINE_FIELD binds HitLocation to AActor in src/, so e.Thing.HitLocation is an invalid/non-existent field access
  5. src/playsim/p_interaction.cpp:1685 — torso is only the implicit fall-through of the if/else-if (head/leg); there is no explicit torso branch or torso multiplier in the live C++ path

ADVERSARIAL: survives=True finalVerdict=partial (conf high)
--- REFUTATION ---
The original 'partial (high)' verdict survives independent re-investigation: the C++ head/leg locational logic is genuinely live and wired on the universal damage path, but the ZScript arbiter's promised true dynamic HitLocation-based bounding is dead code (invalid Actor field access) and torso is not an explicit live case.
 --- ADVERSARY EVIDENCE ---
  1. src/playsim/p_interaction.cpp:1661 — live locational block in DoDamageMobj gated by vr_locational_damage, monster/player, non-explosion, non-melee; reached from universal P_DamageMobj (1736->1749/1733)
  2. src/playsim/p_interaction.cpp:1663-1671 — head/leg cylinder from target->Height, zone by inflictorZ vs bounds, real multipliers vr_locational_head_mult(2.0)/leg_mult(0.5) applied (hw_vrmodes.cpp:395-396)
  3. wadsrc/static/zscript/engine/vr_locational_damage.zs:24 — accesses e.Thing.HitLocation.Z, but e.Thing is native readonly Actor (events.zs:78) which has no HitLocation field = invalid/dead
  4. wadsrc/static/zscript/actors/actor.zs:54 — HitLocation is a member of struct FLineTraceData (struct 36-62); class Actor only starts at :77; only DEFINE_FIELD_X binds it to FLineTraceData (vmthunks_actors.cpp:2258), never to AActor
  5. src/playsim/p_interaction.cpp:1669-1684 — only head and leg if/else-if in live C++; no explicit torso branch/multiplier (torso only exists in the dead ZScript arbiter at vr_locational_damage.zs:74-80)

####################################################################################################
CLAIM ID: locdmg.3  |  CLUSTER: Locational Damage
CLAIM: Audio cues per hit location

INVESTIGATOR VERDICT: absent (conf high)

--- INVESTIGATOR REASONING ---
There are no per-hit-location audio cues on any live path. In the live C++ implementation the head and leg branches produce only floating MSDF text, with zero sound calls. The single S_Sound in the vicinity ('misc/critical', p_interaction.cpp:1699) is bound to the independent random critical-hit roll (vr_crit_chance), which is orthogonal to hit location — it can fire on a torso hit and does not fire specifically for a head/leg hit. The only genuinely location-tied sound ('vr/critical_hit' on a headshot kill) exists exclusively in the dead ZScript VRLocationalArbiter, and it is doubly non-functional: the handler is broken by the invalid HitLocation field access, and the sound alias itself is not defined in sndinfo.txt. I looked for any SNDINFO alias or S_Sound/A_StartSound keyed to head/leg/torso and found none, so the per-location audio cue capability is absent.

--- INVESTIGATOR EVIDENCE ---
  1. src/playsim/p_interaction.cpp:1669-1684 — the live headshot/legshot branches emit ONLY visual MSDF text (FVRMSDFTextThinker::SpawnText 'HEADSHOT!'/'LEGSHOT!'); there is NO S_Sound/audio call in either location branch
  2. src/playsim/p_interaction.cpp:1699 — the only sound in this region, S_Sound(...,'misc/critical'), lives in the SEPARATE random-crit system (vr_crit_chance block at 1688), not the locational head/leg block, so it is not a per-location cue
  3. wadsrc/static/sndinfo.txt — grep for 'vr/critical_hit' (the ZScript headshot-kill sound at vr_locational_damage.zs:56) returns nothing: the sound alias is undefined, so even if that dead handler ran it would play silence
  4. wadsrc/static/zscript/engine/vr_locational_damage.zs:56 — the only per-location A_StartSound ('vr/critical_hit') exists solely in the DEAD ZScript path, which never executes due to the invalid e.Thing.HitLocation access (locdmg.1)
  5. wadsrc/static/sndinfo.txt:28 — searched SNDINFO for headshot/legshot/torso/locational audio aliases; only 'misc/critical->railgf1' exists, and it belongs to the crit system, not hit location

ADVERSARIAL: survives=True finalVerdict=absent (conf high)
--- REFUTATION ---
The absent verdict survives: no per-hit-location audio cue runs on any live path.
 --- ADVERSARY EVIDENCE ---
  1. src/playsim/p_interaction.cpp:1669-1684 — live head/leg location branches call only FVRMSDFTextThinker::SpawnText (visual); grep of 1660-1701 shows the only S_Sound is the crit block, none in the location branches
  2. src/playsim/p_interaction.cpp:1688-1699 — S_Sound('misc/critical') is gated by (M_Random()/65535.0 < vr_crit_chance), a random roll independent of hit location; can fire on torso hits, so not a per-location cue
  3. src/events.cpp:601-609 — InitStaticHandlers registers ONLY classes in gameinfo.EventHandlers; VRLocationalArbiter is absent from the AddEventHandlers list (wadsrc mapinfo/common.txt:8 and pk3 mapinfo/common.txt:8), so the class is never instantiated and its A_StartSound never runs
  4. wadsrc/static/sndinfo.txt:28 (and pk3 sndinfo.txt:28) — only 'misc/critical  railgf1' exists; grep for 'vr/critical_hit'/headshot/legshot/torso/locational returns nothing, so the location-tied alias is undefined
  5. build/Release/doomxr.pk3:zscript/engine/vr_locational_damage.zs:56 — the shipped pk3 copy still declares VRLocationalArbiter as an unregistered StaticEventHandler with the same dead A_StartSound('vr/critical_hit'); pk3 does not diverge from source

####################################################################################################
CLAIM ID: misc.3  |  CLUSTER: Misc Systems (music, menus, monster mass, self-nav)
CLAIM: Mass/weight engine assignment for monsters and their inheritors

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
This is fully wired end-to-end. Stock GZDoom Mass already works (native int Mass, default 100, per-monster overrides like Mass 600, consumed by thrust/knockback in p_map.cpp). On top of that DoomXR adds a keyword-driven mass override: the 'keywords' property is registered on the Actor base class (so every inheritor gets it), Keywords is populated from class defaults and UDMF map merge (p_mobj.cpp:6304), PostBeginPlay() calls ResolveKeywordMass on every spawn and overrides Mass, the dispatcher parses a numeric 'mass:N' token, and Init() + a shipped KEYWORDS.json make the system live. I did find one incomplete sub-path: the named-tier profiles (mass:light/heavy/boss) parse the JSON key 'mass' at keyword_dispatcher.cpp:114 while KEYWORDS.json actually stores 'mass_max', so those named tiers resolve to mass=0 and fail the '>0' check — but the primary numeric mass:N path and stock Mass both work, so the core claim is working.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/scripting/thingdef_properties.cpp:608 — DEFINE_PROPERTY(keywords, S, Actor) registers 'keywords' on the Actor base class so ALL inheritors inherit it via defaults->Keywords
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_mobj.cpp:5210 — AActor::PostBeginPlay() calls KeywordDispatcher::ResolveKeywordMass(Keywords, kw_mass) on EVERY actor spawn and assigns Mass = kw_mass (a live path for all monsters/inheritors)
  3. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:233 — ResolveKeywordMass parses a 'mass:N' token via std::stoi and returns it, real consuming logic (also falls back to profile.mass lookup)
  4. E:/DoomXR-work/DOOM_FRESH/src/d_main.cpp:3656 — KeywordDispatcher::Init() is called at startup and KEYWORDS.json (3425 bytes) is shipped in doomxr.pk3, so the profiles table is populated at runtime
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_map.cpp:6657 — Mass is consumed by knockback physics (thrust = points*0.5/thing->Mass) and thing-pushing (p_map.cpp:7024), so both stock and keyword-assigned Mass actually affect gameplay

ADVERSARIAL: survives=True finalVerdict=working (conf high)
--- REFUTATION ---
I independently re-opened every cited file and traced the chain against running code, trying to break the "working" verdict. The wiring holds. (1) The `keywords` property IS registered on the Actor base class (thingdef_properties.cpp:608-612, writes defaults->Keywords), so every inheritor carries it. (2) `Keywords` is a real FString field on AActor (actor.h:1682), populated on spawn: StaticSpawn creates the thinker from the class (copying default Keywords) and the map-thing keyword is merged in at p_mobj.cpp:6304-6308. (3) On EVERY spawn the thinker system calls CallPostBeginPlay (dthinker.cpp:610/655) → AActor::PostBeginPlay (p_mobj.cpp:5200) which calls ResolveKeywordMass and, on success, assigns Mass (p_mobj.cpp:5210-5213) — this is a live, universal path, not a stub or dead branch. (4) ResolveKeywordMass genuinely parses a numeric `mass:N` token via std::stoi and returns it (keyword_dispatcher.cpp:233-240) — real consuming logic. (5) Init() is called at startup (d_main.cpp:3656) and KEYWORDS.json is shipped inside doomxr.pk3, so the table is populated. (6) Mass is consumed by many live gameplay paths, not just declared: radius-thrust knockback (p_map.cpp:6657), thing-pushing comparisons (p_map.cpp:7024/7091), mass-scaled melee damage (p_map.cpp:926/1581/2850) and gravity thresholds (p_map.cpp:3801). I tried the opposite direction — hunting for a missing link (unregistered property, uncalled function, actor never constructed from defaults, cvar gate) — and found none; the only gate is `kw_mass>0`/non-empty Keywords, which is correct behavior. I DID confirm the one flaw the original auditor honestly disclosed: the named tiers (mass:light/medium/heavy/boss) are dead because the parser reads JSON key "mass" (keyword_dispatcher.cpp:114) while KEYWORDS.json stores "mass_max" (75/300/800/999999), so prof.mass stays 0 and fails the >0 check at line 243. That breaks only the named-tier convenience sub-path; the primary numeric mass:N override and stock native Mass both work fully. Net: end-to-end working with a minor incomplete sub-path — the original "working/high" verdict survives.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/scripting/thingdef_properties.cpp:608-611 — DEFINE_PROPERTY(keywords,S,Actor) writes defaults->Keywords on the Actor base class, so all monster inheritors carry it
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_mobj.cpp:5210-5213 — AActor::PostBeginPlay() calls ResolveKeywordMass(Keywords,kw_mass) and assigns Mass=kw_mass; reached on every spawn via CallPostBeginPlay driven by dthinker.cpp:610/655
  3. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:233-240 — ResolveKeywordMass parses numeric 'mass:N' via std::stoi and returns true; this primary path is functional
  4. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_map.cpp:6657,7024,1581 — Mass is actually consumed by thrust/knockback, thing-pushing and mass-scaled melee damage, so both stock and keyword-set Mass affect gameplay
  5. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:KEYWORDS.json (mass namespace uses 'mass_max' 75/300/800/999999) vs keyword_dispatcher.cpp:114 reading key 'mass' — confirms named tiers resolve to 0 and are dead, but numeric mass:N path is unaffected

####################################################################################################
CLAIM ID: misc.1  |  CLUSTER: Misc Systems (music, menus, monster mass, self-nav)
CLAIM: Doom dynamic music mod built in

INVESTIGATOR VERDICT: absent (conf high)

--- INVESTIGATOR REASONING ---
I searched both repos and the shipped pk3 for any combat/state-reactive or dynamic music system. What exists is entirely stock GZDoom: the zmusic library, per-map MAPINFO 'music=' assignments, the MusicChanger map-thing (a vanilla feature), and a volume slider. The only combat-reactive code (gitd_glow.zs CombatLevel/react-combat) modulates lighting glow, not music, and contains no S_ChangeMusic call. I looked specifically for a MUSDEF parser, a music-intensity/combat state machine, an EventHandler that swaps tracks on WorldThingDied/combat, and a dynamic-music lump in the pk3 — none exist. There is no dynamic music mod built in; only stock, static music infrastructure.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/mapinfo/common.txt:225 — the only 'dynamic' music is stock GZDoom's MusicChanger map-thing (editor #14101-14119), a per-thing music swap, not a combat-reactive music engine
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp (grep) — no MUSDEF/dynamicmusic/MusicIntensity/combatmusic parser exists anywhere in src/; music is handled only by the stock zmusic library
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd_glow.zs:414 — the only 'react-combat' logic (CombatLevel()) drives GLOW brightness, not music; it never calls S_ChangeMusic
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/doombase.zs:139 — S_ChangeMusic is only the native declaration; its sole ZScript caller is conversationmenu.zs:282 (SetMusicVolume for Strife dialog), no game-state-driven music switching
  5. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3 (unzip -l) — no dynamic-music lump, MUSDEF, jukebox, or dynmus entry; menudef only exposes the stock snd_musicvolume slider

ADVERSARIAL: survives=True finalVerdict=absent (conf high)
--- REFUTATION ---
See above.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/doombase.zs:139 — S_ChangeMusic exists only as a native static declaration; a full-tree grep found zero ZScript callers of it (no game-state music switching)
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd_glow.zs:414-419 — the only 'react-combat' logic, CombatLevel(), scales glow brightness (base*(0.5+0.5*combat*mul)); it never calls S_ChangeMusic
  3. E:/DoomXR-work/DOOM_FRESH/src/common/audio/music/i_music.cpp — grep for musdef/dynamic/combat/intensity returns nothing; music is the stock zmusic interface, no dynamic/combat-reactive parser exists in src/
  4. E:/_gitd35_repo/zmapinfo — addeventhandlers registers 10 handlers (GITD_DeathFXHandler, HF_GlowHandler, DarkDoomZ_Handler, GITD_FlashHandler, GITD_PresetHandler, GITD_BloomHandler, GITD_MuzzleHandler, gy_EventHandler, gy_VmAbortHandler, VRBlackoutHandler) — none touch music; grep of zscript_gitd/zscript_gy for ChangeMusic/SetMusic returns nothing
  5. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3 — unzip listing shows no MUSDEF/jukebox/dynmus lump and no music tracks; the only .ogg/.flac audio are ds* sound EFFECTS (dsbounce/dsdgatk/dssecret), and menudef exposes only the stock snd_musicvolume slider

####################################################################################################
CLAIM ID: misc.2  |  CLUSTER: Misc Systems (music, menus, monster mass, self-nav)
CLAIM: Unlocked dual-cursor menus (both controllers usable as menu cursors)

INVESTIGATOR VERDICT: absent (conf high)

--- INVESTIGATOR REASONING ---
The VR menu pointer/cursor is real and wired, but it is strictly single-cursor: both the OpenXR (vk_openxrdevice) and OpenVR (gl_openvr) paths hardcode the right controller (pointerHand=1 / controllers[1]) as the only menu ray source, with a single cursor overlay and a single CVar pair. I looked specifically for a second pointerHand/cursor overlay handle, a left-hand menu-ray branch, and any 'dual'/'two cursors'/'second cursor' code — none exist. The gearbox mod has off-hand WEAPON WHEELS (zscript_gearbox/gearbox/event_handler.zs:316) but that is a weapon selector, not a menu cursor. Nothing implements dual-cursor menus, so the specific claim of 'unlocked dual-cursor menus' is absent (a single-cursor menu pointer works).

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/vulkan/stereo3d/vk_openxrdevice.cpp:3521 — 'const int pointerHand = 1; // Always use the right controller for virtual menu mouse.' hardcodes a SINGLE hand for the OpenXR menu ray
  2. E:/DoomXR-work/DOOM_FRESH/src/rendering/gl/stereo3d/gl_openvr.cpp:3440 — comment 'OpenVR virtual menu mouse (right controller)'; the ray origin uses controllers[1] only (gl_openvr.cpp:3459-3463), no left-hand path
  3. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/vulkan/stereo3d/vk_openxrdevice.cpp:137 — a single vr_menu_pointer / vr_menu_pointer_color CVar pair; there is no second-cursor CVar or second beam/cursor overlay handle
  4. E:/DoomXR-work/DOOM_FRESH/src/ (grep 'dual cursor|two cursors|second cursor|dualcursor') — zero matches across the entire engine source; only one overlayCursorHandle exists (gl_openvr.cpp:363)
  5. E:/DoomXR-work/DOOM_FRESH/src/rendering/gl/stereo3d/gl_openvr.cpp:1670 — a single 'doomVRCursorOverlay' overlay is created; there is no second cursor overlay for the off-hand

ADVERSARIAL: survives=True finalVerdict=absent (conf high)
--- REFUTATION ---
Verdict survives. The VR menu cursor is strictly single-hand, right-controller only. OpenXR uses const int pointerHand = 1 (never reassigned, never reads vr_control_scheme). OpenVR uses controllers[1]/rightControllerValid with no left branch. Exactly one overlayCursorHandle exists; only one vr_menu_pointer CVar pair. The gearbox offhand wheel is a weapon selector, not a menu cursor. No dual/second-cursor code in engine, pk3, or GITD mod. Dual-cursor menus are absent.</refutation>
</invoke>

 --- ADVERSARY EVIDENCE ---
  1. vk_openxrdevice.cpp:3521 — const int pointerHand = 1 hardwires the OpenXR menu ray to the right controller, never reassigned, never reads vr_control_scheme
  2. gl_openvr.cpp:3451-3463 — OpenVR menu-mouse reads controllers[1] only, gated on rightControllerValid, no left-hand branch
  3. gl_openvr.cpp:363 — sole overlayCursorHandle declaration; only one doomVRCursorOverlay created (line 1670), no second cursor
  4. menudef:3311 — single vr_menu_pointer/vr_menu_pointer_color pair; vr_control_scheme 10/11 are left-handed schemes not a dual-cursor mode
  5. zscript_gearbox/gearbox/event_handler.zs:316 — offhand wheel is a weapon selector, not a menu cursor

####################################################################################################
CLAIM ID: misc.4  |  CLUSTER: Misc Systems (music, menus, monster mass, self-nav)
CLAIM: BETA: self-navigating actors (SDFs/engine-drawn glows) that auto-navigate toward player-selected goals (keys, doors), respond to player course changes, then resume their course

INVESTIGATOR VERDICT: absent (conf high)

--- INVESTIGATOR REASONING ---
I searched both repos and the pk3 for a self-navigating actor that pathfinds toward player-selected goals (keys/doors), reacts to player course changes, and resumes. No such system exists. The single candidate is GITD_SDFImp (an SDF-rendered 'engine-drawn glow' imp), but it is a DoomImp subclass that relies entirely on stock Doom chase AI toward its target=player; it has no goal-selection, no key/door targeting, and no course-change/resume logic. The only goal-navigation primitives present (actor->goal, PatrolPoint, InterpolationPoint, A_Wander, Strife follower AI) are unmodified stock GZDoom for cameras/patrols and are not wired to player-selected keys or doors. I specifically looked for waypoint/escort/reroute/resume-course code and any binding of navigation to keys/doors and found none, so this BETA feature is absent.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd_sdfmonster.zs:1 — the only SDF/glow 'monster', GITD_SDFImp, is a plain DoomImp subclass; its Tick() only tweaks matrix_glitch/msdf_* shader vars and uses inherited stock chase-AI toward its target (the player), with no goal selection
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd_sdfmonster.zs:61 — comment 'for now we’ll just keep it as a new actor they can spawn' confirms it is not even a replacement, let alone a navigation system
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/ src/ (grep 'key.*goal|door.*goal|route.*door|seek.*key|navigateto') — zero real matches binding actor navigation to player-selected keys or doors; hits are language strings/cvar config keys
  4. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_enemy.cpp:283 — the only navigation-goal concept is stock GZDoom actor->goal (used with PatrolPoint/InterpolationPoint via A_Chase); it is unmodified vanilla and not tied to keys/doors or a resume-course mechanic
  5. E:/_gitd35_repo (grep 'navigat|pathfind|escort|companion|resume|reroute|toward.*key|goalactor' --include=*.zs) — zero matches in the GITD mod for any self-navigation/resume-course behavior

ADVERSARIAL: survives=True finalVerdict=absent (conf high)
--- REFUTATION ---
The original 'absent' verdict survives my adversarial re-investigation; no navigation-to-goal system for keys/doors exists in code, and the sole SDF monster is neither a navigator nor even instantiated.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd_sdfmonster.zs:25 — GITD_SDFImp (only SDF 'monster') Tick() only writes msdf_glitch/msdf_enabled/msdf_color shader vars; navigation is inherited stock DoomImp chase-AI toward target=player, no goal selection/key/door/resume logic
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_lnspec.cpp:1834 — LS_Thing_SetGoal resolves goals via GetActorIterator(NAME_PatrolPoint, arg1) ONLY; the sole goal-nav primitive is unmodified vanilla, never bound to keys or doors and has no resume-course mechanic
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd_sdfmonster.zs:1 — grep for GITD_SDFImp/SDFImp across DOOM_FRESH wadsrc, _gitd35_repo, and doomxr.pk3 finds zero spawn/replace/eventhandler/mapinfo call sites: the class is defined but never instantiated on any live path
  4. E:/_gitd35_repo (grep -rniE 'navigat|pathfind|escort|companion|reroute|resume.course|selfnav|autonav|goalactor' — no matches) — the GITD mod contains no self-navigation, course-change, or resume-course code of any kind
  5. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_sprites.cpp:114 — msdf_* IS a real consumed shader path (SetMSDFParams), so engine-drawn glow entities exist, but every SDF entity (VRSigilGraphic/sdf_gif/damage_counters/gy_Stone) is a static or float-up display with no navigation attached

####################################################################################################
CLAIM ID: recoil.4  |  CLUSTER: Recoil, Bullet-Drop, Two-Handing & Stabilization
CLAIM: Stabilization mechanic: two-hand a weapon to offset recoil

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
The stabilization mechanic is fully wired end to end. Each tic P_PlayerThink calls VR_CalculateTwoHanding (p_user.cpp:1437), which measures the distance between the two VR hand transforms and sets vr_two_hand_stabilized when within radius. That flag is read by the VR device layer (vk_openxrdevice.cpp:3301, gl_openvr.cpp:2418) into weaponStabilised, which P_SetPsprite converts into the WF_TWOHANDSTABILIZED weapon-state flag (p_pspr.cpp:551-553), and which also gates the offhand-button lockout (p_pspr.cpp:849). The recoil offset is real: VR_ApplyRecoil multiplies aim climb by vr_stabilization_recoil_mult when weaponStabilised (hw_vrmodes.cpp:1153). The only caveat is that the recoil-offset benefit flows through the VR_ApplyRecoil climb path that the shipped weapons don't feed (recoil.2), but the stabilization detection, the WF flag, and the recoil-multiplier logic are all present on live paths, so the mechanic itself is working.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1305 — VR_CalculateTwoHanding computes hand distance and sets vr_two_hand_stabilized=true when hands are within radius
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1437 — VR_CalculateTwoHanding is called every tic from P_PlayerThink, a live path
  3. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/vulkan/stereo3d/vk_openxrdevice.cpp:3301 — render/input layer reads weaponStabilised = vr_two_hand_stabilized (also gl_openvr.cpp:2418), which P_SetPsprite turns into WF_TWOHANDSTABILIZED (p_pspr.cpp:551-553)
  4. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:1153 — recoil is actually offset when stabilized: if (weaponStabilised) climb *= vr_stabilization_recoil_mult (default 0.5)
  5. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:483 — cvars vr_two_handed_weapons/vr_twohand_radius are real and read by the gate at p_user.cpp:1309/1313

ADVERSARIAL: survives=True finalVerdict=working (conf high)
--- REFUTATION ---
I re-opened every cited file and traced the mechanic from detection to consumption, then hunted for a missing consumer that would demote it. Detection is real and live: VR_CalculateTwoHanding (p_user.cpp:1307) sets player->vr_two_hand_stabilized when hand distance is within vr_twohand_radius, and it is called unconditionally every tic from P_PlayerThink (defined p_user.cpp:1347, invoked p_user.cpp:2207), no #ifdef guards in that region. The gate cvar vr_two_handed_weapons defaults TRUE (hw_vrmodes.cpp:482). The flag is genuinely consumed: both VR device layers assign the global weaponStabilised from it (vk_openxrdevice.cpp:3301, gl_openvr.cpp:2418) and use it to compute a two-handed aim-stabilization solve for weaponangles, plus extra fire haptics (p_map.cpp:4773, p_mobj.cpp:7643). P_SetPsprite turns it into WF_TWOHANDSTABILIZED (p_pspr.cpp:553), which is CONSUMED by live ZScript (player.zs:393 FireWeapon, weapons.zs:423) and C++ (p_pspr.cpp:849) to lock the offhand out while it supports the weapon. My strongest attack was the recoil-climb multiplier itself: the stabilization multiply (climb *= vr_stabilization_recoil_mult, hw_vrmodes.cpp:1153) lives in VR_ApplyRecoil, which has exactly ONE caller, A_Recoil (p_actionfunctions.cpp:1322), gated by cvar vr_recoil which defaults FALSE (p_actionfunctions.cpp:91), and no shipped weapon in wadsrc/static calls A_Recoil (only the native declaration exists, actor.zs:1259). So that specific recoil-number reduction is dormant by default. But that is exactly the recoil.2 caveat already flagged, and it does not sink THIS claim: the stabilization mechanic (two-hand to steady the weapon) is wired end-to-end through detection, the aim-stabilization solve, the WF flag, and the offhand lockout, all on default-on live paths. The original working/high verdict survives.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1307 — VR_CalculateTwoHanding sets vr_two_hand_stabilized=true when hands within radius (set at line 1331); called every tic from live P_PlayerThink (defined 1347, invoked 2207), no ifdef, gate vr_two_handed_weapons defaults TRUE (hw_vrmodes.cpp:482)
  2. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/vulkan/stereo3d/vk_openxrdevice.cpp:3301 — weaponStabilised = vr_two_hand_stabilized, then used to compute the two-handed aim-stabilization weaponangles solve (mirror gl_openvr.cpp:2418); this is the real steady-the-aim effect, independent of vr_recoil
  3. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_pspr.cpp:553 — sets WF_TWOHANDSTABILIZED; consumed by live ZScript FireWeapon (player.zs:393), weapons.zs:423, and C++ P_CheckWeaponButtons (p_pspr.cpp:849) to lock the supporting offhand out; runs with vr_recoil OFF
  4. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:1153 — recoil-climb multiplier: if(weaponStabilised) climb *= vr_stabilization_recoil_mult (default 0.5, p_actionfunctions.cpp:97); real logic but the weakest link
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_actionfunctions.cpp:1322 — VR_ApplyRecoil's ONLY caller is A_Recoil, gated by cvar vr_recoil default FALSE (line 91); no shipped weapon calls A_Recoil (only native decl actor.zs:1259) so the recoil-number reduction is dormant-by-default (the recoil.2 caveat), not a break of stabilization itself

####################################################################################################
CLAIM ID: recoil.1  |  CLUSTER: Recoil, Bullet-Drop, Two-Handing & Stabilization
CLAIM: Expanded and refined per-shot recoil and stabilization

INVESTIGATOR VERDICT: partial (conf high)

--- INVESTIGATOR REASONING ---
Two distinct recoil systems coexist. The ZScript A_VRRecoil (vr_weapon_helpers.zs:37) is genuinely wired and runs per shot from real weapon fire states (shotgun/ssg/pistol/bfg/plasma/rlaunch), so per-shot recoil is live, and stabilization is also live (recoil.4). However the 'refined' engine-side recoil (VR_ApplyRecoil, hw_vrmodes.cpp:1144, with separated visual kickback offset and cumulative aim-climb) is only invoked through native A_Recoil, and I searched the entire wadsrc zscript tree and found NO weapon state calling A_Recoil, only the native prototype at actor.zs:1259. So the shipped weapons use the crude direct-pitch nudge while the refined engine model sits unreachable. Partial: real per-shot recoil plus real stabilization, but the refined engine recoil is bypassed by the shipped weapons.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/engine/vr_weapon_helpers.zs:37 — A_VRRecoil() is defined and directly nudges player.mo.pitch/angle per shot, gated by cvar vr_recoil
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/doom/weaponshotgun.zs:36 — live weapon fire state calls A_VRRecoil(3.0), proving the simple per-shot recoil path actually runs
  3. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:1144 — refined C++ VR_ApplyRecoil() splits visual kickback offset from aim-climb, but is only reachable via native A_Recoil
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/actor.zs:1259 — A_Recoil is only a native declaration; grep of the entire zscript tree finds ZERO weapon states calling A_Recoil, so the refined recoil path is never triggered
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_actionfunctions.cpp:1322 — A_Recoil redirects to VR_ApplyRecoil when VR active, confirming the refined path is wired but depends on an uncalled action

ADVERSARIAL: survives=True finalVerdict=partial (conf high)
--- REFUTATION ---
The partial verdict survives independent re-investigation. See detailed reasoning in the refutation field.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/engine/vr_weapon_helpers.zs:37-45 — A_VRRecoil defined, nudges player.mo.pitch/angle per shot gated by CVar vr_recoil; called from 7+ live fire paths (shotgun/ssg/pistol/bfg/plasma/rlaunch/smg + A_BallisticFire)
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/actor.zs:1259 — A_Recoil is ONLY a native declaration; grep of full zscript tree AND every .zs entry in build/Release/doomxr.pk3 finds ZERO weapon states calling it, so the refined recoil is never triggered
  3. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_actionfunctions.cpp:1311-1322 — A_Recoil routes to VR_ApplyRecoil only when vr_recoil && VR active, confirming the refined path depends entirely on the uncalled A_Recoil
  4. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:915,991,1153 — VR_ApplyRecoil output IS consumed on live paths (AttackPitch/Angle, weapon transform, weaponStabilised*vr_stabilization_recoil_mult) and decayed via VR_UpdateRecoil at p_user.cpp:1353, so the refined model is wired-but-uninvoked, not absent
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_actionfunctions.cpp:91 — CVAR(Bool, vr_recoil, false, ...) defaults BOTH recoil systems OFF (menu-toggleable at menudef.txt:3070/3373/4350), so even the live per-shot path is not on by default — reinforcing partial over working

####################################################################################################
CLAIM ID: recoil.5  |  CLUSTER: Recoil, Bullet-Drop, Two-Handing & Stabilization
CLAIM: Two-handing adjusted per-keyword for true front-grip positioning with a reactive VR hand pose

INVESTIGATOR VERDICT: partial (conf high)

--- INVESTIGATOR REASONING ---
This claim splits in two. The reactive VR hand pose IS real and wired: two-handing sets the offhand vr_hand_state to Grip (p_user.cpp:1445), which models.cpp:222-238 uses to swap the VRHandModel to its defined 'Grip' state (vr_hand.zs:16). But 'per-keyword true front-grip positioning' is NOT achieved. GetWeaponOffsets fetches ox/oy/oz (p_user.cpp:1313-1314) but those variables are never referenced again; the stabilization uses raw hand-to-hand distance and the code comment admits 'For now we just use distance'. Worse, the lookup itself is dead: profiles are keyed 'weapons:Shotgun' (keyword_dispatcher.cpp:175) while weapons only carry 'class:shotgun' tokens, and I confirmed by grep that NO weapon carries any 'weapons:' token. Partial: reactive pose works; per-keyword front-grip position is fetched-but-discarded and keyed to non-existent tokens.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/r_data/models.cpp:222 — reactive hand pose IS wired: RenderHUDModel reads vr_hand_state[hand] and at :227 maps state 1 to the 'Grip' state of VRHandModel, swapping the rendered hand model
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1445 — when vr_two_hand_stabilized, the offhand vr_hand_state is set to 1 (Grip), so two-handing drives the reactive grip pose
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/shared/vr_hand.zs:16 — VRHandModel actually defines Grip/Climb/Point states (real asset backing the pose)
  4. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1313 — front-grip offset (ox,oy,oz) is fetched via GetWeaponOffsets but then NEVER used; the comment at :1325-1326 says 'For now we just use distance between hands'
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:175 — profiles are keyed 'weapons:'+name (e.g. weapons:Shotgun), but weapons carry tokens like 'class:shotgun' (weaponshotgun.zs:18), so GetWeaponOffsets' token lookup never matches

ADVERSARIAL: survives=True finalVerdict=partial (conf high)
--- REFUTATION ---
The "partial" bucket survives, but I attacked the reactive-pose half they called "working" and found it is gated OFF by default, which they missed. The per-keyword front-grip half is confirmed dead and is worse than reported: GetWeaponOffsets (keyword_dispatcher.cpp:303) does profiles.find(token) on raw weapon tokens, but weapon profiles are keyed "weapons:Shotgun" (kd.cpp:175) while weapons carry "class:shotgun" (weaponshotgun.zs:18) — I grep-confirmed ZERO "weapons:" tokens exist in any ZScript, so the lookup never matches. Additionally the KEYWORDS.json weapons entries (lines 82-85) contain recoil_force/twohand_radius/parry_extent but NO twohand_offset_x/y/z fields at all, so even a matching lookup yields ox=oy=oz=0. The ox/oy/oz are fetched at p_user.cpp:1313-1314 and never read again (comment at :1326: "For now we just use distance between hands"). Now the half they over-credited: the ONLY setter of vr_two_hand_stabilized=true is p_user.cpp:1331 inside VR_CalculateTwoHanding, which early-returns at :1315 when (vr_twohand_whitelist_only && !isWeapon). vr_twohand_whitelist_only DEFAULTS TO TRUE (hw_vrmodes.cpp:484) and isWeapon is the always-false GetWeaponOffsets return, so with shipped defaults two-handing NEVER stabilizes, meaning the offhand Grip pose at p_user.cpp:1445 never fires from two-handing. The pose-swap machinery itself is genuine and would run (models.cpp:222-238 reads vr_hand_state, maps 1->"Grip"; vr_hand.zs:16 defines the Grip state; modeldef.txt FrameIndex VHAN B->frame 1 with real vhand.iqm/vhand.png assets in the pk3), and the Grip state is still reachable via held-items/climbing branches (p_user.cpp:1441-1444). So real wiring exists but the claim's headline mechanic — two-handing adjusted per-keyword driving a reactive front-grip pose — is broken end-to-end by default: per-keyword offsets never resolve and the two-hand grip pose is blocked by the default-true whitelist. Verdict remains partial; the reactive pose is not the clean "works" the prior auditor implied.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:484 — vr_twohand_whitelist_only defaults to TRUE, and p_user.cpp:1315 early-returns when whitelist && !isWeapon, so two-hand stabilization never engages by default (prior auditor missed this gate)
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:309 — GetWeaponOffsets calls profiles.find(token) on raw tokens; profiles are keyed 'weapons:Shotgun' (kd.cpp:175) but weapons carry 'class:shotgun' (weaponshotgun.zs:18); grep found ZERO 'weapons:' tokens in any zscript, so it always returns false
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/KEYWORDS.json:82 — weapons profiles define recoil_force/twohand_radius/parry_extent but NO twohand_offset_x/y/z, so even a matching lookup would yield ox=oy=oz=0; front-grip offset data does not exist
  4. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1331 — the sole setter of vr_two_hand_stabilized=true sits after the whitelist early-return, so with default cvars the offhand Grip pose at :1445 never triggers from two-handing
  5. E:/DoomXR-work/DOOM_FRESH/src/r_data/models.cpp:222 — the reactive pose machinery IS real (reads vr_hand_state, maps 1->'Grip' at :227; backed by vr_hand.zs:16 Grip state and modeldef VHAN B->vhand.iqm), but the Grip state is only reachable via held-items/climbing (p_user.cpp:1441-1444), not two-handing by default

####################################################################################################
CLAIM ID: recoil.2  |  CLUSTER: Recoil, Bullet-Drop, Two-Handing & Stabilization
CLAIM: Advanced recoil including CUMULATIVE kickback

INVESTIGATOR VERDICT: declared_unwired (conf high)

--- INVESTIGATOR REASONING ---
A genuine cumulative kickback model exists in the engine: vr_recoil_pitch_accum/yaw_accum accumulate across shots (hw_vrmodes.cpp:1155-1157), are consumed by the live firing aim (AttackPitch/AttackAngle at :915/:917), and decay over tics (:1183). But it is only fed by VR_ApplyRecoil, which is only called from native A_Recoil (p_actionfunctions.cpp:1322). I grepped every zscript file and found A_Recoil has zero weapon call sites, only its native declaration at actor.zs:1259. The weapons instead call A_VRRecoil, which applies a stateless per-shot pitch nudge with no accumulation. So the cumulative kickback is fully implemented and internally wired, but nothing on a live weapon path drives it: declared_unwired.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:1155 — VR_ApplyRecoil accumulates: vr_recoil_pitch_accum += climb (true cumulative aim climb, random yaw drift at :1157)
  2. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:915 — accumulator is folded into the live firing aim: AttackAngle += vr_recoil_yaw_accum, and :917 AttackPitch -= vr_recoil_pitch_accum
  3. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:1183 — decay logic (pitch_accum *= 0.9 after reset_tic) confirms a cumulative-with-decay model
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/actor.zs:1259 — the only feeder of VR_ApplyRecoil is native A_Recoil, which no weapon state calls (grep = zero call sites)
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/engine/vr_weapon_helpers.zs:43 — the recoil the weapons actually use (A_VRRecoil) does player.mo.pitch -= intensity per shot with NO accumulator, i.e. not cumulative

ADVERSARIAL: survives=True finalVerdict=declared_unwired (conf high)
--- REFUTATION ---
The original declared_unwired verdict survives. The cumulative kickback model (accumulators + decay, consumed by firing aim) is fully implemented but its only feeder, native A_Recoil, is called by no weapon anywhere (engine, mod, or baked pk3); weapons use the stateless A_VRRecoil instead.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:1155 — vr_recoil_pitch_accum += climb; the cumulative model is real and its accumulators are written only here (and :1157), consumed at :915/:917, decayed at :1183
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_actionfunctions.cpp:1322 — VR_ApplyRecoil(player,hand,xyvel) is the sole caller of the accumulator writer, and it lives only inside the native A_Recoil action function
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/actor.zs:1259 — A_Recoil exists only as 'action native void A_Recoil(double xyvel)'; grep across engine wadsrc, GITD mod, and baked pk3 finds ZERO weapon call sites of A_Recoil
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/engine/vr_weapon_helpers.zs:43 — the recoil weapons actually invoke (A_VRRecoil) does 'player.mo.pitch -= intensity * frandom(0.8,1.2)' per shot with NO accumulator; called by pistol/shotgun/plasma/etc
  5. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3 (zscript/actors/actor.zs & zscript/engine/vr_weapon_helpers.zs) — freshly-baked pk3 matches source exactly: A_Recoil only as native decl, A_VRRecoil call sites at pk3 helpers :102/:226 and weaponpistol :37 — confirms no live driver in shipped content

####################################################################################################
CLAIM ID: recoil.3  |  CLUSTER: Recoil, Bullet-Drop, Two-Handing & Stabilization
CLAIM: Keyword-based bullet drop

INVESTIGATOR VERDICT: declared_unwired (conf high)

--- INVESTIGATOR REASONING ---
The physics side is real: p_mobj.cpp:2647 applies extra Z-velocity drop to missiles based on vr_metadata.bullet_drop. But that value can never become non-zero via keywords. The JSON stores bullet_drop under a 'ballistics' namespace (KEYWORDS.json:76-80), yet Init (keyword_dispatcher.cpp:59-188) has no branch for 'ballistics', so no profile gets a bullet_drop. Even if it did, ResolveMetadata (which populates actor->vr_metadata at p_mobj.cpp:5059) doesn't copy the bullet_drop field. The GetBulletDrop() accessor has zero callers. The only 'drop' that actually happens is vanilla Actor Gravity on the ZScript BallisticBullet (vr_weapon_helpers.zs:230, Gravity 0.15) plus per-fire gravity args in A_BallisticFire, which is standard projectile gravity, NOT keyword-driven. Keyword-based bullet drop is declared but unwired.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_mobj.cpp:2647 — real physics consumer exists: if ((flags & MF_MISSILE) && vr_metadata.bullet_drop > 0) Vel.Z -= gravity*bullet_drop
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:59 — Init parses color/climb/mass/actor/pickup/flags/weapons namespaces but has NO parser for 'ballistics', so bullet_drop is never loaded from JSON
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/KEYWORDS.json:76 — the JSON defines bullet_drop only under a 'ballistics' namespace that the C++ parser ignores entirely
  4. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:250 — ResolveMetadata (called at p_mobj.cpp:5059 to fill vr_metadata) copies role/trait/faction/etc but never copies bullet_drop, so vr_metadata.bullet_drop stays 0
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:321 — GetBulletDrop() exists but has ZERO callers anywhere in src/ (verified by grep)

ADVERSARIAL: survives=True finalVerdict=declared_unwired (conf high)
--- REFUTATION ---
I re-opened every cited file and independently traced the full chain, then hunted for wiring the original auditor might have missed (an alternate C++ parser, a ZScript-side setter, a native VM export, a pk3-baked variant, and a live CVar gate). Every path is a dead end, so the "declared_unwired" verdict survives — if anything it is stronger than stated. Confirmed the physics consumer is real (p_mobj.cpp:2647-2650 subtracts gravity*bullet_drop from missile Vel.Z, gated on vr_metadata.bullet_drop > 0.0f). But that field can never be non-zero via keywords: (1) Init parses only color/climb/mass/actor/pickup/flags/weapons namespaces (keyword_dispatcher.cpp:59-188) with NO 'ballistics' branch — the only 'ballistics' token in all of src is a comment in the header, so profiles[...].bullet_drop is never assigned from KEYWORDS.json:76-80; the sole assignment to bullet_drop anywhere is the default '= 0.0f' initializer (keyword_dispatcher.h:34). (2) ResolveMetadata (keyword_dispatcher.cpp:250-273) — the exact function that populates actor->vr_metadata at the StaticSpawn call site p_mobj.cpp:5059 — copies role/trait/faction/head_height/score_set/item_type/rarity/value/points but NEVER copies bullet_drop, so even a hypothetical non-zero profile value could not reach vr_metadata. (3) GetBulletDrop (keyword_dispatcher.cpp:321) has zero callers in src (grep confirmed only the definition). NEW dead-ends I found that reinforce the verdict: the CVar vr_bullet_drop_enabled (p_actionfunctions.cpp:100, default true) is exposed in the menu (menudef.txt:3433) but is read by ZERO acting logic — the physics branch doesn't even gate on it; and there is no ZScript path that sets bullet_drop (the only ZScript 'drop' is vanilla Gravity on BallisticBullet, standard projectile physics, not keyword-driven). Keyword-based bullet drop is defined at both the JSON and physics ends but the middle is severed in two places.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:59-188 — Init parses color/climb/mass/actor/pickup/flags/weapons namespaces but has NO 'ballistics' branch (grep: only reference is a '// Ballistics' comment in the .h), so bullet_drop is never loaded from KEYWORDS.json
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:250-273 — ResolveMetadata (the function that fills actor->vr_metadata) copies role/trait/faction/head_height/score_set/item_type/rarity/value/points but NEVER copies bullet_drop, so vr_metadata.bullet_drop stays at its 0.0f default
  3. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_mobj.cpp:5059 — actor->vr_metadata is populated solely via KeywordDispatcher::ResolveMetadata(actor->Keywords, actor->vr_metadata) at StaticSpawn; confirms no other populator sets bullet_drop
  4. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:321 — GetBulletDrop() is defined but has ZERO callers in src (grep across src/ returns only this definition line)
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_actionfunctions.cpp:100 & menudef.txt:3433 — CVar vr_bullet_drop_enabled is declared and shown in the menu but read by NO acting logic (grep: declaration + menu row only); the p_mobj.cpp:2647 physics branch doesn't even check it

####################################################################################################
CLAIM ID: recoil.6  |  CLUSTER: Recoil, Bullet-Drop, Two-Handing & Stabilization
CLAIM: Per-weapon keyword-adjustable foregrip location/positioning (removes the hardcoded two-hand range)

INVESTIGATOR VERDICT: declared_unwired (conf high)

--- INVESTIGATOR REASONING ---
The intent is to replace the hardcoded vr_twohand_radius with per-weapon values (and per-weapon foregrip offsets). It does not work in practice. The dispatcher keys weapon profiles as 'weapons:Shotgun' (keyword_dispatcher.cpp:175), but weapons only carry 'class:shotgun'-style tokens (weaponshotgun.zs:18) and I verified by grep that zero weapons carry any 'weapons:' token, so GetWeaponOffsets' per-token lookup at p_user.cpp:1314 always fails, isWeapon is always false, and the code always falls back to the hardcoded vr_twohand_radius. On top of the token mismatch, the JSON 'weapons' entries define NO twohand_offset_x/y/z fields (KEYWORDS.json:82-85 only have twohand_radius/recoil_force/parry_extent), so the foregrip location has no data even if the key matched, and (per recoil.5) the fetched offset is never applied. The hardcoded range is therefore never removed. Declared_unwired.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1313 — radius defaults to the hardcoded cvar vr_twohand_radius; GetWeaponOffsets is meant to override it per-weapon
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:175 — Init stores weapon profiles under key 'weapons:'+name (e.g. 'weapons:Shotgun')
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/doom/weaponshotgun.zs:18 — the Shotgun's Keywords are 'class:shotgun' etc.; grep confirms NO weapon anywhere carries a 'weapons:' token, so GetWeaponOffsets' profiles.find(token) never hits
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/KEYWORDS.json:81 — the JSON 'weapons' entries only provide twohand_radius/recoil_force/parry_extent; they define NO twohand_offset_x/y/z, so the foregrip location has no data source even if matched
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/keyword_dispatcher.cpp:180 — the parser only reads twohand_offset_* / twohand_radius, ignoring recoil_force and parry_extent present in the JSON, so the weapons namespace is only half-consumed

ADVERSARIAL: survives=True finalVerdict=declared_unwired (conf high)
--- REFUTATION ---
I attacked the verdict from the opposite direction — hunting for wiring the auditor missed (an alias token, a native side, a different data source, or an application of the offsets elsewhere) — and found none; every link in their chain reproduced independently. (1) Token mismatch: grep for 'weapons:' across the entire wadsrc/static tree returns ZERO matches, and the Shotgun carries 'class:shotgun' (weaponshotgun.zs:18), not 'weapons:Shotgun'. The dispatcher builds profile keys as \"weapons:\"+name (keyword_dispatcher.cpp:175), so profiles.find(token) in GetWeaponOffsets (line 309) can never match any weapon's real Keywords token. (2) No data: the JSON weapons block (identical in source and in the pk3-baked doomxr.pk3) defines only recoil_force/twohand_radius/parry_extent — grep for 'twohand_offset' across all content returns ZERO — so even a key match would read default-zero offsets. (3) NEW independent finding: even the fetched values are discarded — ox/oy/oz appear ONLY at p_user.cpp:1313-1314 and are never used again; the stabilization test at line 1327 (distSq = (p1-p2).LengthSquared()) uses raw hand positions with no foregrip offset, and the comment at 1325-1326 admits \"For now we just use distance between hands.\" (4) radius is only overwritten when isWeapon is true, which never happens, so it always stays vr_twohand_radius and the hardcoded range is never removed. The feature is scaffolded (function + JSON namespace + call site) but has zero live consumer. Verdict stands.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static — grep 'weapons:' returns ZERO matches across all ZScript/keyword source, while dispatcher keys profiles as 'weapons:'+name (keyword_dispatcher.cpp:175); the per-token lookup in GetWeaponOffsets can never hit any real weapon token
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/doom/weaponshotgun.zs:18 — Shotgun Keywords are 'class:shotgun'/'mass:40'/etc., carrying NO 'weapons:' token to match a profile
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/KEYWORDS.json:82-85 — weapons entries provide only recoil_force/twohand_radius/parry_extent; grep 'twohand_offset' returns ZERO, so foregrip offsets have no data source (source and pk3-baked JSON identical)
  4. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1313-1327 — ox/oy/oz are fetched into locals at 1313-1314 then NEVER used; distSq at 1327 uses raw (p1-p2) with no offset applied, and comment 1325-1326 admits 'For now we just use distance between hands'
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1313,1315-1316 — radius stays vr_twohand_radius unless isWeapon (which requires the never-present 'weapons:' match) is true, so the hardcoded range is never removed

####################################################################################################
CLAIM ID: score.1  |  CLUSTER: SDF Combat Patterns & Keyworded Score Tracker
CLAIM: 30+ types of hit and combat patterns drawn diegetically in-game

INVESTIGATOR VERDICT: partial (conf medium)

--- INVESTIGATOR REASONING ---
The pattern SHADERS genuinely exist and are wired to a real native draw primitive (AddGlowPanel -> main.fp wgType branches): ~11 air-panel shapes + number panel in main.fp and ~8-9 SDF sigil primitives in vr_sdf_procedural.fp, so the raw drawable-primitive inventory approaches but I could not cleanly confirm reaches 30 without counting hash-combination variants. It is only 'partial' because the delivery path is dead: nearly all combat shapes (dampop/ring/disc/casing/shard/smoke/skull) are spawned only by GITD_DamPop/GITD_ComboTag/GITD_Brand, whose sole driver GITD_ComboHandler is a non-static EventHandler NOT listed in AddEventHandlers (common.txt:8), so it never receives WorldThingDamaged and those shapes never draw. A few shapes are reachable via the MSDF-sprite path, but that path is broken (see score.3). I searched for any alternate registration or caller of GITD_ComboHandler and found none.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:shaders/glsl/main.fp:906-1214 — ~11 distinct AddGlowPanel air-panel shape branches exist (14 ring, 15 disc, 16 casing, 17 shard, 18 corner-brackets, 19 waveform, 20 smoke, 21 gauge, 22 spectrum, 23 skull, 26 lightning) plus wgType==13 number panel
  2. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:shaders/glsl/vr_sdf_procedural.fp:63-118 — a second pattern set via SDF primitives (circle/box/hex/star + slab/obelisk/cross/scanline) selected by keyword hash
  3. E:/DoomXR-work/DOOM_FRESH/src/scripting/vmthunks.cpp:1184 — AddGlowPanel is a real native LevelLocals function feeding the main.fp shape zoo, so that draw path is genuinely wired
  4. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/radiance/gitd_combo.zs:119 — the ONLY handler spawning shape-zoo actors (GITD_DamPop/GITD_ComboTag) is GITD_ComboHandler, a plain EventHandler
  5. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:mapinfo/common.txt:8 — AddEventHandlers list omits GITD_ComboHandler, so on the live path the damage-driven shapes never spawn

ADVERSARIAL: survives=False finalVerdict=partial (conf high)
--- REFUTATION ---
The auditor's LABEL (partial) is correct but their REASONING is substantially wrong, so the verdict as-argued does not survive. Their thesis was "the delivery path is dead: nearly all combat shapes never draw because their sole driver GITD_ComboHandler is a non-static EventHandler missing from AddEventHandlers." I confirmed that specific fact (common.txt:8 omits GITD_ComboHandler; it is a plain EventHandler in gitd_combo.zs:119; no alternate registration exists anywhere in the pk3), so the Brand/BrassStorm/Buckshot/DamPop/combo cluster IS dead. BUT the auditor MISSED that two OTHER handlers listed IN AddEventHandlers (common.txt:8) draw the same shape zoo on live, default-ON paths. (1) GITD_DeathFXHandler is registered and its WorldThingDied dispatches 14 distinct death-effect shape classes via PickEffect/StyleClass (gitd3_deathfx.zs:1155-1191: DeathPool/SeamReveal/GhostWalk/DeathPing/StylizedX/HexField/HexRings/Spiral/Detector/Firework/SquareRings/Star/Sunburst/Grid), plus its WorldThingSpawned bullet-puff hook dispatches ~11 impact-shape classes (ImpactClass, gitd3_deathfx.zs:1250-1266) AND 6 HF spark-burst types (HF_ImpType1-6), plus a kill-combo number (wipeType 13), heatmap thermal cells, and hit-lights — all default-enabled (cvarinfo: gitd_death_enabled=true, gitd_impact_style=1). (2) GITD_JunkerScanHandler is registered and its WorldTick draws ~7 air-panel shapes (skull/brackets/ring/waveform/spectrum/gauge/digit) default-ON (gitd_junker_demo=true, gitd_junker_enabled=true). All shapes decode in real shader branches (main.fp:906-1214 air panels wgType 14-26; main.fp ~1250-1560 floor cascade wgType 0-13) fed by genuine native primitives (vmthunks.cpp:1157 AddGlowSpotWiped, :1184 AddGlowPanel). So the drawable-pattern inventory well exceeds 30 and MOST of it is on a live default-on path — the opposite of the auditor's "path is dead." It remains partial only because a real named subset IS dead: the weapon-signature combat effects (Brand/Brass Storm/Buckshot), per-hit DamPop, and cumulative combo meter never spawn (unregistered driver), and the SDF sigil path (SDFComboArbiter is a StaticEventHandler so it auto-registers and fires WorldThingDied → SpawnSigil, vr_sdf_combos.zs:12/62/114 — genuinely live code-side) renders through the MSDF vr_sdf_procedural.fp shader that is part of the known launch crash. Net: partial, but because a specific cluster is dead, not because "nearly all shapes never draw."
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/radiance/gitd3_deathfx.zs:1155-1191 — REGISTERED GITD_DeathFXHandler.WorldThingDied dispatches 14 distinct death-effect shape classes via PickEffect/StyleClass on every kill (default gitd_death_enabled=true), a live diegetic combat-pattern path the auditor missed
  2. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/radiance/gitd3_deathfx.zs:1079-1108,1250-1266 — same registered handler's WorldThingSpawned bullet-puff hook dispatches ~11 ImpactClass glow-shapes plus 6 HF_ImpType spark bursts on every hit (default gitd_impact_style=1), fully wired live
  3. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:mapinfo/common.txt:8 — AddEventHandlers DOES include GITD_DeathFXHandler and GITD_JunkerScanHandler (both draw the shape zoo), so the delivery path is largely LIVE; only GITD_ComboHandler (combo/Brand/BrassStorm/Buckshot/DamPop) is omitted and thus dead
  4. E:/DoomXR-work/DOOM_FRESH/src/scripting/vmthunks.cpp:1157,1184 — AddGlowSpotWiped and AddGlowPanel are real native LevelLocals primitives feeding both the floor cascade (main.fp wgType 0-13) and air panels (main.fp wgType 14-26), confirming the shape zoo genuinely renders
  5. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/ui/vr_sdf_combos.zs:12,62,114 — SDFComboArbiter is a StaticEventHandler (auto-registers without AddEventHandlers) whose WorldThingDied calls VRSigilManager.SpawnSigil: code-side live, but its render path is the MSDF vr_sdf_procedural.fp shader tied to the known launch crash

####################################################################################################
CLAIM ID: score.2  |  CLUSTER: SDF Combat Patterns & Keyworded Score Tracker
CLAIM: Keyword + location-damage combat system tied to a dynamic, real-time, keyworded SDF Score Tracker

INVESTIGATOR VERDICT: partial (conf medium)

--- INVESTIGATOR REASONING ---
The wiring skeleton is real, not a stub: SDFComboArbiter and VRLocationalArbiter are auto-registering StaticEventHandlers, the keyword table drives actual branching (distance/klass/zone/hand are consumed, not just tabled), and lastHitZone/lastHitHand are real Actor fields added via 'extend class Actor'. That is why it is not 'declared_unwired'. But the location half is broken at its root: the headshot/legshot classifier computes zRatio from e.Thing.HitLocation.Z, and AActor has no HitLocation member in ZScript (it lives only on the unrelated FLineTraceData struct) or in C++ (grep of actor.h and all DEFINE_FIELDs found none on AActor). That is a compile-fatal member access that would abort ZScript VM init, and it also reads undeclared cvars vr_locational_head_mult / vr_locational_leg_mult (absent from CVARINFO) that would VM-abort at runtime. So the location-damage input the tracker depends on is non-functional; the keyword tracker is coded and hooked but fed by a broken locational stage.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/ui/vr_sdf_combos.zs:12 — SDFComboArbiter is a StaticEventHandler (self-registers) overriding WorldThingDied, so its kill hook is genuinely live
  2. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/ui/vr_sdf_combos.zs:73,87-107 — it builds a context string (tag,zone,hand), does a real distance<64 check, real inflictor.GetClassName() klass check, substring matches on head/legs/hand:off, then calls VRSigilManager.SpawnSigil
  3. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/engine/vr_locational_damage.zs:8,50-51,65 — VRLocationalArbiter (StaticEventHandler) is the code that populates lastHitZone/lastHitHand the tracker consumes
  4. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/engine/vr_locational_damage.zs:24 — its zone detection reads e.Thing.HitLocation.Z, but HitLocation is NOT an Actor field
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/actor.zs:36-62,77 — HitLocation is declared only inside struct FLineTraceData (36-62); Actor class opens at 77 with no HitLocation, and no DEFINE_FIELD(AActor,HitLocation) exists (vmthunks_actors.cpp binds it only on FLineTraceData at 2258)

ADVERSARIAL: survives=False finalVerdict=partial (conf high)
--- REFUTATION ---
The verdict word "partial" happens to still fit, but the auditor's justification is wrong on both halves: the keyworded SDF score tracker is NOT a live auto-registered handler (it is never registered in any mapinfo/gameinfo and has no RegisterNew, so it is inert dead code), while the locational-damage stage they called "broken/non-functional" is actually implemented and live as a native C++ system in DoDamageMobj. The specific claimed capability — a keyworded, real-time SDF Score Tracker tied to location-damage — is declared_unwired.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/events.cpp:590-621 — InitStaticHandlers registers static handlers ONLY from gameinfo.EventHandlers and MAPINFO's EventHandlers list; a class merely deriving StaticEventHandler does NOT auto-register
  2. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:mapinfo/common.txt:8 — AddEventHandlers lists 11 handlers, NONE of them SDFComboArbiter or VRLocationalArbiter; no RegisterNew anywhere → WorldThingDied/WorldThingDamaged never fire (tracker is dead code)
  3. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/actors/actor.zs:54,77 — HitLocation is a field of struct FLineTraceData (opens :36); class Actor opens :77 with no HitLocation, so vr_locational_damage.zs:24 e.Thing.HitLocation.Z is a compile-fatal unknown-member access (codegen.cpp:7140)
  4. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_interaction.cpp:1660-1685 — a native C++ locational-damage system inside DoDamageMobj (reached via P_DamageMobj :1749) gates on vr_locational_damage, applies vr_locational_head_mult/leg_mult, and spawns MSDF HEADSHOT!/LEGSHOT! text — this works and is what the auditor missed
  5. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:392,395-396 — vr_locational_damage/head_mult/leg_mult are real native CVARs (defaults true/2.0/0.5), refuting the auditor's claim that these cvars are undeclared and would VM-abort

####################################################################################################
CLAIM ID: score.4  |  CLUSTER: SDF Combat Patterns & Keyworded Score Tracker
CLAIM: SDF cumulative damage counters drawn as player-facing billboards

INVESTIGATOR VERDICT: partial (conf high)

--- INVESTIGATOR REASONING ---
This is the most fully wired claim: VRDamageHandler is a self-registering StaticEventHandler (no missing mapinfo entry, unlike GITD_ComboHandler), it genuinely accumulates per-target cumulative damage and spawns/refreshes a +BILLBOARD VRDamageCounter, the actor's damageTotal is pushed to the native msdf_enabled field, the engine forwards those fields to the shader for every sprite (hw_sprites.cpp:114), the VRDMGA0 sprite lump actually exists, and gldefs binds it to vr_damage_sdf.fp which renders the value as SDF 7-segment digits. Every link in this specific chain is present and uses only fields/functions that exist. I withhold full 'working' (partial) for one reason: this system shares the ZScript VM with VRLocationalArbiter's compile-fatal e.Thing.HitLocation access (score.2) — if that access aborts ZScript compilation, the entire mod VM including VRDamageHandler fails to load. That shared-VM build-integrity dependency I could not rule out statically, and it aligns with the known launch issue. Absent that break, the damage-counter billboard path is genuinely and completely wired.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/ui/vr_damage_counters.zs:58 — VRDamageHandler is a StaticEventHandler (self-registers) whose WorldThingDamaged accumulates counter.damageTotal per target and resets a lifeTimer (real cumulative tracking, live hook)
  2. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/ui/vr_damage_counters.zs:1-10 — VRDamageCounter is a +BILLBOARD actor (camera/player-facing) that sets msdf_enabled=damageTotal and follows the target above its head
  3. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_sprites.cpp:114 — the engine unconditionally forwards actor->msdf_enabled/glitch/color into SetMSDFParams for every drawn sprite, so the total reaches the shader uniforms end-to-end
  4. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:gldefs.txt:1-3 — sprite VRDMGA0 has a real lump (sprites/VRDMGA0.png) and is bound via HardwareShader Sprite VRDMGA0 -> vr_damage_sdf.fp, which decodes u_IsMSDF into 3 SDF digits (vr_damage_sdf.fp:46-54)
  5. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript.txt:20 — vr_damage_counters.zs is #included, confirming it is compiled into the build

ADVERSARIAL: survives=True finalVerdict=partial (conf high)
--- REFUTATION ---
Partial survives but I confirmed the shared-VM compile break. All wiring is real: VRDamageCounter is a BILLBOARD actor writing damageTotal into native msdf_enabled, VRDamageHandler self-registers and accumulates cumulative per-target damage, fields are DEFINE_FIELD'd and forwarded to the shader on the live sprite path at hw_sprites.cpp:114, VRDMGA0 lump exists and gldefs binds it to vr_damage_sdf.fp. BUT the whole mod VM fails to compile: vr_locational_damage.zs:24 does e.Thing.HitLocation.Z where e.Thing is an Actor, and HitLocation is a member of struct FLineTraceData (actor.zs:54, struct opens line 36) not class Actor (opens line 77). C++ binds it only as DEFINE_FIELD_X(FLineTraceData) at vmthunks_actors.cpp:2258; no AActor binding. This compile-fatal unknown-identifier aborts the single-unit ZScript VM, so VRDamageHandler never registers and no billboard draws in the shipped build. Complete wiring, dead at runtime.</refutation>
</invoke>

 --- ADVERSARY EVIDENCE ---
  1. actor.zs:36,54,77 HitLocation is inside struct FLineTraceData not class Actor
  2. vr_locational_damage.zs:24 e.Thing.HitLocation.Z on an Actor is compile-fatal aborting the whole VM
  3. vmthunks_actors.cpp:2258 HitLocation bound only to FLineTraceData no AActor binding
  4. hw_sprites.cpp:114 SetMSDFParams forwards actor msdf_enabled unconditionally would work if VM compiled
  5. doomxr.pk3 vr_locational_damage.zs:24 shipped pk3 carries the identical break

####################################################################################################
CLAIM ID: score.3  |  CLUSTER: SDF Combat Patterns & Keyworded Score Tracker
CLAIM: The tracker reads context like 'Imp, headshot, revolver, close range, last bullet in clip, offhand shot' and plays pre-programmed SDF score reactions in real time

INVESTIGATOR VERDICT: partial (conf high)

--- INVESTIGATOR REASONING ---
The claim's specific context list is only partially represented and the represented parts are broken. 'Imp' tag = yes (GetTag); 'headshot' = coded but depends on the compile-fatal HitLocation access; 'offhand shot' = coded (hand:off from Weapon.bOffhandWeapon) but set only inside the same broken locational handler; 'close range' = partially real (a live distance<64 check) but most distance tiers are dead-substring; 'revolver'/weapon = broken class-name matching that won't fire; 'last bullet in clip' = ABSENT — I searched the arbiter and the whole context pipeline and found no ammo/clip read at all. Even on a match, the SDF reaction is VRSigilManager.SpawnSigil, whose actor needs sprite SIGLA0 which does not exist in the pk3 (only VRDMGA0), so it would render nothing. I checked for a JSON-driven fuller table (sdf_combos.json exists) but no ZScript parses it — the arbiter hardcodes a smaller set in OnRegister. Real hook + real partial context, but several claimed inputs are absent/broken and the visual output sprite is missing.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/ui/vr_sdf_combos.zs:73 — fullContext = victim.GetTag() + lastHitZone + lastHitHand ONLY; it has monster tag, head/legs/torso, and hand:main/off — no range, no weapon, and no clip/ammo token is ever placed in it
  2. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/ui/vr_sdf_combos.zs:87-88 — only 'distance < 64' and 'distance > 512' are numeric-checked; table entries like 'distance > 1024' fall to substring matching and can never match, so most range combos are dead
  3. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/ui/vr_sdf_combos.zs:107 — weapon match is inflictor.GetClassName()!=c.klass against names like 'fist'/'rifle'/'revolver'; for hitscans inflictor is not the weapon and no weapon classes are named these, so weapon combos won't fire; NO code anywhere reads ammo/clip (the 'last bullet in clip' input does not exist)
  4. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/engine/vr_locational_damage.zs:24 — the head/legs zone feeding 'headshot' depends on e.Thing.HitLocation (nonexistent Actor field, compile-fatal per score.2), so headshot/offhand context is not reliably produced
  5. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:gldefs.txt:11-14 — VRSigilManager spawns VRSigilGraphic using sprite SIGL (frame A0 -> SIGLA0) bound to vr_sdf_procedural.fp, but NO SIGLA0 sprite lump exists in the pk3 (only VRDMGA0), so the sigil reaction has no sprite to render

ADVERSARIAL: survives=False finalVerdict=declared_unwired (conf high)
--- REFUTATION ---
See above.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/actor.zs:36,54,77 — HitLocation is a member of struct FLineTraceData (opened line 36, field line 54), and class Actor starts at line 77 with NO HitLocation field; thus e.Thing.HitLocation (Actor) at vr_locational_damage.zs:24 is a compile-fatal nonexistent-member access that aborts the whole ZScript unit, so SDFComboArbiter never runs
  2. doomxr.pk3:zscript/ui/vr_sdf_combos.zs:73 — fullContext = victim.GetTag()+lastHitZone+lastHitHand only; no weapon, no range, no ammo token; a whole-pk3 .zs scan for ammo/clip/magazine/'last bullet' returned zero hits, so 'last bullet in clip' input is absent everywhere
  3. doomxr.pk3:zscript/ui/vr_sdf_combos.zs:87-88,107 — only distance<64 and distance>512 are numeric-checked (table's distance>1024 is dead), and weapon match uses inflictor.GetClassName()!=c.klass with lowercase 'revolver'/'rifle' while GetClassName() returns capitalized 'Revolver' (case-sensitive compare) — weapon+range combos cannot fire
  4. doomxr.pk3:CVARINFO — vr_locational_head_mult and vr_locational_leg_mult are read at vr_locational_damage.zs but NOT declared here (grep returned nothing), a runtime null-deref even if the file compiled
  5. doomxr.pk3:gldefs.txt + sprites/ — gldefs binds HardwareShader Sprite SIGLA0 and VRSigilGraphic.Tick sets sprite='SIGL', but only sprites/VRDMGA0.png exists (no SIGLA0 lump), so the sigil reaction has no sprite to render

####################################################################################################
CLAIM ID: sdfcore.3  |  CLUSTER: SDF/MSDF Rendering Core & MSDF-as-Actor
CLAIM: Per-actor MSDF granularity: every entity can possess unique msdf_enabled / msdf_glitch / msdf_color, flowing through the Vulkan StreamData UBO

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
This is the one claim that is genuinely wired end-to-end. Per-actor storage exists on AActor, is script-exposed, is read from the concrete drawn actor in both the world-sprite (hw_sprites) and HUD-weapon (hw_weapon) paths, written into mStreamData, uploaded via ApplyStreamData into the Vulkan StreamData UBO, and the UBO's GLSL layout byte-matches the C++ struct and is consumed by a real shader. I verified the upload call site (not just the setter) and the matching UBO field order/padding. The only caveat is that the per-actor value only has a visible effect when the actor's sprite has a gldefs MSDF shader binding (otherwise main.fp ignores it), but the granular per-actor data flow through the UBO itself is real and complete.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/playsim/actor.h:1301-1303 — per-actor fields float msdf_enabled/msdf_glitch + FVector3 msdf_color exist on AActor (true per-entity granularity), exposed to ZScript via vmthunks_actors.cpp:2064-2066 DEFINE_FIELD and actor.zs:221-223 native declarations.
  2. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_sprites.cpp:114 — DrawSprite reads the drawn actor's fields and calls state.SetMSDFParams(actor->msdf_enabled, actor->msdf_glitch, actor->msdf_color) per sprite, so each actor's values flow into render state.
  3. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_renderstate.h:670-674 — SetMSDFParams writes mStreamData.u_IsMSDF/u_MSDFGlitch/u_MSDFColor, and vk_renderstate.cpp:206,351,362 ApplyStreamData()->mStreamBufferWriter.Write(mStreamData) uploads the whole StreamData to the UBO each draw.
  4. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/vulkan/shaders/vk_shader.cpp:272-277,423-425 — GLSL StreamData UBO declares u_IsMSDF/u_MSDFGlitch/padding2/padding3/u_MSDFColor identical to the C++ struct (hw_renderstate.h:224-229) and #defines them for shader use, so the std140 layout matches byte-for-byte.
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/shaders/glsl/vr_damage_sdf.fp:47,65,69 — a bound shader actually consumes u_IsMSDF, u_MSDFColor and u_MSDFGlitch, closing the loop from actor field to fragment output.

ADVERSARIAL: survives=True finalVerdict=working (conf high)
--- REFUTATION ---
Verdict survives. Full end-to-end wiring confirmed.
 --- ADVERSARY EVIDENCE ---
  1. proof one
  2. proof two

####################################################################################################
CLAIM ID: sdfcore.4  |  CLUSTER: SDF/MSDF Rendering Core & MSDF-as-Actor
CLAIM: Infinitely drawable, customizable, scalable in-world vector graphics with infinite resolution and color, interactive

INVESTIGATOR VERDICT: partial (conf medium)

--- INVESTIGATOR REASONING ---
The design is coded: a registered handler reacts to kills, spawns a customizable procedural-SDF actor, and the shader is genuinely scalable vector math reading the per-actor uniforms and player cvars. So the interactive+customizable+scalable logic is really present, not vaporware. However it does not actually draw in-world: the sigil actor references sprite 'SIGL' which has no lump, so the SIGLA0 gldefs shader binding never fires and the actor renders nothing (or a null sprite). The parallel C++ in-world vector-text/impact particle path is dead because the MSDF font assets directory is empty and GetFontAsset returns null. I searched for a SIGL/SIGLA0 sprite lump and for any populated vr_assets/msdf content and found neither. Hence: real, live wiring but blocked at the final asset/lump step -> partial.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/ui/vr_sdf_combos.zs:12,62,114 — SDFComboArbiter (StaticEventHandler, auto-registers) fires on WorldThingDied and calls VRSigilManager.SpawnSigil, an interactive kill-reactive spawn path that is genuinely live.
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/ui/vr_sdf_procedural.zs:34-36,97 — VRSigilGraphic sets msdf_enabled/glitch/color from customizable cvars (vr_sigil_color_*, vr_sigil_base_complexity) and is spawned, so the customization/interactivity logic exists.
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/shaders/glsl/vr_sdf_procedural.fp:50-121 — resolution-independent procedural SDF (circle/box/hex/star/graveyard) driven by the uniforms, i.e. real 'infinite resolution' vector math.
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/ui/vr_sdf_procedural.zs:30 + find(SIGL) — the spawned sigil uses sprite 'SIGL' but NO SIGLA0/SIGL sprite lump exists anywhere, so the gldefs binding (gldefs.txt:11 Sprite SIGLA0) never attaches and the actor cannot render its shader.
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/vr_msdf_text.cpp:158,380 + empty vr_assets/msdf — the C++ in-world impact/text vector particles route through GetFontAsset which returns null (no font assets), so SpawnImpact/SpawnText no-op at runtime.

ADVERSARIAL: survives=True finalVerdict=partial (conf high)
--- REFUTATION ---
The partial verdict survives independent re-investigation.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_sprites.cpp:1213,1216 — DrawSprite returns early when the sprite patch/texture is invalid; since no SIGL lump exists the VRSigilGraphic sprite is invalid, so the sigil renders nothing in-world (the broken half)
  2. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3 — sprites/VRDMGA0.png exists (real 3x2 PNG) but NO SIGLA0/SDFGA0 sprite lump exists in any loaded archive, while gldefs.txt:11 binds the SDF shader to sprite SIGLA0 that never loads
  3. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_sprites.cpp:114 — SetMSDFParams(actor->msdf_enabled,glitch,color) is called in the live sprite draw path, and vk_shader.cpp:423-425 binds u_IsMSDF/u_MSDFGlitch/u_MSDFColor, so per-actor customization plumbing is genuinely wired GPU-side (the real half)
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/ui/vr_sdf_combos.zs:12,62,114 — SDFComboArbiter is a StaticEventHandler (auto-registers) whose WorldThingDied fires VRSigilManager.SpawnSigil, a live interactive kill-reactive spawn that reads cvars vr_sigil_* (CVARINFO:14-18)
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/vr_msdf_text.cpp:157-158 — the parallel C++ in-world text path is called from live sites (p_interaction.cpp:1674/1682/1695, p_mobj.cpp:7925) but GetFontAsset returns null (vr_assets/msdf dirs empty) so SpawnText returns nullptr and renders nothing

####################################################################################################
CLAIM ID: sdfcore.1  |  CLUSTER: SDF/MSDF Rendering Core & MSDF-as-Actor
CLAIM: Driven entirely by SDFs, divorced from sprite-based weapons and enemies (sprite-less rendering possible)

INVESTIGATOR VERDICT: declared_unwired (conf high)

--- INVESTIGATOR REASONING ---
The engine has no sprite-less render path for SDF. Every SDF/MSDF shader is a gldefs 'HardwareShader Sprite <LUMP>' binding, which requires a real sprite lump to be drawn before the shader runs; the SDF actors literally set a sprite index and use the quad as a shader canvas. The universal sprite shader main.fp never touches the MSDF uniforms, so conventional sprite rendering is untouched and dominant. I looked for a global/opt-out sprite path or a render branch selecting SDF from u_IsMSDF independent of sprite name and found none. The concept is coded as a per-sprite shader override, not as SDF-driven sprite-less rendering.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/gldefs.txt:1,6,11 — MSDF shaders bind ONLY to specific sprite lumps (Sprite VRDMGA0/SDFGA0/SIGLA0), i.e. rendering is keyed to a sprite, NOT sprite-less; SDF activates only when that exact sprite is drawn.
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/shaders/glsl/main.fp — the DEFAULT sprite/geometry shader contains NO reference to u_IsMSDF/u_MSDF*, so all normal actors/weapons/enemies render conventionally; nothing is 'divorced' from sprites.
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/ui/vr_sdf_procedural.zs:30 — VRSigilGraphic still does sprite=GetSpriteIndex("SIGL"); frame=0, i.e. the SDF entity is a sprite actor that borrows a sprite as a shader bounding-quad, not sprite-less.
  4. find over E:/DoomXR-work/DOOM_FRESH — only sprites/VRDMGA0.png exists; no SIGL/SDFG sprite lump, so two of the three SDF shaders can never bind.

ADVERSARIAL: survives=True finalVerdict=declared_unwired (conf high)
--- REFUTATION ---
The original declared_unwired verdict survives. I independently confirmed the SDF/MSDF system is a per-sprite shader override, not a sprite-less render path: no weapon sets msdf_enabled, the SDF enemy (GITD_SDFImp) is never spawned and depends on a missing SIGL sprite lump, two of three SDF shaders can never bind (SIGL/SDFG lumps absent from source and pk3), and the runtime spawner handlers (SDFComboArbiter, VRDamageHandler) are not in common.txt AddEventHandlers so they never fire. The real C++ uniform plumbing (SetMSDFParams -> u_IsMSDF) only modulates already-drawn sprite/HUD quads, which contradicts the "sprite-less, divorced from sprites" claim.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd_sdfmonster.zs:22 — GITD_SDFImp:DoomImp does sprite=GetSpriteIndex("SIGL"); the SDF enemy is a sprite-bound actor borrowing a lump as a shader quad, and has no 'replaces' clause and no spawner anywhere (never SDF-driven in-game)
  2. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3 — unzip -l shows only sprites/VRDMGA0.png; SIGL and SDFG sprite lumps exist in neither the pk3 nor the source tree, so the SIGLA0/SDFGA0 HardwareShader Sprite bindings in gldefs.txt:6,11 can never bind
  3. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_sprites.cpp:114 — SetMSDFParams is called INSIDE HWSprite::DrawSprite (and hw_weapon.cpp:125 DrawPSprite); MSDF is a per-sprite/HUD shader modulation requiring a sprite to draw, i.e. sprite-BOUND, not a sprite-less render path
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/mapinfo/common.txt:8 — AddEventHandlers lists 11 handlers but NOT SDFComboArbiter or VRDamageHandler; both are StaticEventHandlers (require explicit MAPINFO registration, cf. listed VRBlackoutHandler) so SpawnSigil (vr_sdf_combos.zs:114) and the damage-counter spawner never fire at runtime
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/actor.zs:221 — msdf_enabled/glitch/color are set only by effect/UI actors (vr_death_effects, vr_damage_counters, gy_Stone, sdf_gif_entity, gitd_sdfmonster, vr_sdf_procedural); grep shows ZERO weapon actors set msdf_enabled, so weapons are not divorced from sprites

####################################################################################################
CLAIM ID: sdfcore.2  |  CLUSTER: SDF/MSDF Rendering Core & MSDF-as-Actor
CLAIM: Vector render of enemies, items, menus, weapons — infinitely scalable, customizable, actor-ready

INVESTIGATOR VERDICT: partial (conf medium)

--- INVESTIGATOR REASONING ---
There is genuine vector-render plumbing that reaches weapons (hw_weapon.cpp), sprites (hw_sprites.cpp) and a working procedural digit shader. But 'enemies, items, menus, weapons' as a delivered vector system is only partially real: the single functioning SDF-shader chain is the damage-counter (VRDMGA0 sprite exists + vr_damage_sdf.fp bound + StaticEventHandler VRDamageHandler spawns it). Enemy vector render (GITD_SDFImp) is never instantiated; the item/menu atlas shader references an undefined screenPxRange() and its sprite lump is missing; and the C++ MSDF text/menu glyph system is asset-starved (empty vr_assets/msdf). So the claim is real in skeleton and for exactly one sprite, but broadly unwired for enemies/items/menus.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_weapon.cpp:125 — SetMSDFParams IS called for HUD weapon sprites (huds->owner->msdf_*), so the weapon path is wired to the uniform, proving actor-ready plumbing exists.
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/shaders/glsl/vr_damage_sdf.fp:10-58 — a real vector (7-segment SDF) digit renderer driven by u_IsMSDF, screen-resolution independent, proving vector render works for at least the damage-counter sprite VRDMGA0 (the one sprite lump that exists).
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd_sdfmonster.zs:1 — GITD_SDFImp (enemy vector render) is DEFINED but has zero spawn/replace callers (grep across wadsrc/src returns only its own definition).
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/shaders/glsl/msdf_atlas.fp — the 'items/menus' atlas path calls screenPxRange() which is defined nowhere in shaders or engine prelude, so it would not compile if loaded; its sprite SDFG also does not exist.
  5. E:/DoomXR-work/DOOM_FRESH/vr_assets/msdf/ — empty directory: no MSDF font/atlas assets ship, so vector text for menus/items has no data.

ADVERSARIAL: survives=False finalVerdict=declared_unwired (conf high)
--- REFUTATION ---
The "partial" verdict overstates it: not one of the four advertised surfaces (enemies, items, menus, weapons) reaches a working end-to-end SDF vector render on a live path. I traced every path myself. gldefs.txt binds three SDF shaders to three sprite lumps (VRDMGA0->vr_damage_sdf.fp, SDFGA0->msdf_atlas.fp, SIGLA0->vr_sdf_procedural.fp); only VRDMGA0.png actually exists (source and pk3), so the atlas and procedural shaders can never bind (no SDFGA0/SIGLA0 lump exists anywhere), and msdf_atlas.fp additionally calls screenPxRange() which is defined nowhere = would not compile if bound. ENEMIES: GITD_SDFImp is never spawned/replaced. ITEMS/GIF: sdf_gif_entity.zs (SDFGifEntity + VRGifEventHandler) is not even #included in zscript.txt, so it does not compile into the game. MENUS/text: the C++ MSDF font manager (FVRMSDFManager::GetFontAsset) reads vr_assets/msdf/*.json via FindFirstFileA and that directory is empty, so GetFontAsset returns null and no glyphs render. WEAPONS/damage-counter (the auditor's claimed one-working-sprite): the live C++ route p_interaction.cpp:1712 -> FVRMSDFDamageCounter::RecordDamage -> SpawnText(FName("Arcade")) renders through that same empty MSDF font system, NOT the working VRDMGA0 digit shader; the only route that would reach the working VRDMGA0 shader is the ZScript VRDamageCounter spawned by VRDamageHandler, and that StaticEventHandler is NOT in the AddEventHandlers list (common.txt registers 11 handlers, none of them VRDamageHandler/SDFComboArbiter/VRGifEventHandler), and the engine registers static handlers ONLY from gameinfo.EventHandlers (events.cpp InitStaticHandlers), so WorldThingDamaged never fires and VRDamageCounter never spawns. The one genuinely live SDF spawn is gy_Stone (spawned by the registered gy_EventHandler), which sets sprite=GetSpriteIndex("SIGL") + msdf_enabled=256 to drive vr_sdf_procedural.fp, but the SIGL/SIGLA0 sprite lump does not exist, so the HardwareShader binding never activates and the actor has no renderable sprite. Net: real skeleton and uniform plumbing exist, but there is zero live, asset-complete SDF vector render of enemies, items, menus, or weapons. That is declared_unwired, not partial.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/mapinfo/common.txt:8 — AddEventHandlers registers 11 handlers, NONE of them VRDamageHandler/SDFComboArbiter/VRGifEventHandler; since events.cpp:601 InitStaticHandlers registers static handlers only from gameinfo.EventHandlers, the SDF damage/sigil/gif WorldThingDamaged/Died handlers never fire and never spawn their actors
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/gldefs.txt:6-13 — msdf_atlas.fp is bound to sprite SDFGA0 and vr_sdf_procedural.fp to SIGLA0, but neither SDFGA0 nor SIGLA0 sprite lump exists in source or build/Release/doomxr.pk3 (only VRDMGA0.png exists), so those two shader bindings can never activate
  3. E:/DoomXR-work/DOOM_FRESH/src/playsim/vr_msdf.cpp:32-39 — the C++ MSDF font/text system loads glyph atlases from progdir vr_assets/msdf/*.json via FindFirstFileA, and that directory is empty, so GetFontAsset returns null; the live damage-counter path p_interaction.cpp:1712 -> RecordDamage -> SpawnText(FName("Arcade")) renders through this asset-starved font system, not the working VRDMGA0 digit shader
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript.txt:19-24 — sdf_gif_entity.zs (SDFGifEntity + VRGifEventHandler, the items/gif surface) is not #included in the zscript compile list at all, so that half never compiles into the game; GITD_SDFImp (enemies) is defined but has zero spawn/replace callers
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gy_Stone.zs:237,249 — the one live SDF spawn (gy_Stone via the registered gy_EventHandler) sets sprite=GetSpriteIndex("SIGL") and msdf_enabled=256 to drive vr_sdf_procedural.fp, but no SIGL/SIGLA0 sprite lump exists (only gy_tomb1-4.png ship), so the shader never binds and the actor has no renderable SDF sprite

####################################################################################################
CLAIM ID: sdfcore.5  |  CLUSTER: SDF/MSDF Rendering Core & MSDF-as-Actor
CLAIM: Sprite-less SDF monsters / procedural entities (spawn blank-canvas actor, render whole entity via fragment-shader SDF math)

INVESTIGATOR VERDICT: declared_unwired (conf high)

--- INVESTIGATOR REASONING ---
The sprite-less SDF-monster concept exists only as unreferenced class definitions. GITD_SDFImp/_Shatter have no spawner, replacement, or summon anywhere, so no such monster ever exists at runtime. They are not even blank-canvas: they set sprite 'SIGL' (a lump that does not exist) and rely on the sprite-keyed gldefs shader binding, contradicting the 'sprite-less' framing. The one procedural entity with a live handler (SDFGifEntity) is only reachable via a manual dev netevent and is doubly broken (missing SDFG sprite lump + atlas shader references an undefined screenPxRange()). The C++ SpawnMSDFBillboard helper is likewise never called. I specifically looked for spawn/replace/summon sites and gameplay callers and found none; this is a declared-but-unwired feature.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd_sdfmonster.zs:1,63 — GITD_SDFImp and GITD_SDFImp_Shatter are defined, but a grep over all of wadsrc and src finds ZERO spawn/replaces/summon/DECORATE references to them; they are never instantiated on any code path.
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd_sdfmonster.zs:22 — even if spawned, it does sprite=GetSpriteIndex("SIGL") (NOT blank-canvas), and no SIGL sprite lump exists, so the SIGLA0 shader binding (gldefs.txt:11) cannot attach.
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/ui/sdf_gif_entity.zs:42-66 — the only 'procedural entity' with a live handler (VRGifEventHandler) spawns SDFGifEntity ONLY from a manual 'summon_gif_sentinel' netevent (dev command), not gameplay, and it uses sprite 'SDFG' which also has no lump; atlas shader msdf_atlas.fp additionally calls undefined screenPxRange().
  4. E:/DoomXR-work/DOOM_FRESH/src/playsim/vr_msdf.cpp:248-272 — FVRMSDFManager::SpawnMSDFBillboard (the C++ 'render whole entity as MSDF' entry) has no callers outside vr_msdf.cpp; grep shows it is never invoked from gameplay code.

ADVERSARIAL: survives=True finalVerdict=declared_unwired (conf high)
--- REFUTATION ---
The original declared_unwired verdict SURVIVES independent re-investigation. See detailed trace in the refutation field.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/mapinfo/common.txt:8 — the ONLY AddEventHandlers line; omits SDFComboArbiter and VRGifEventHandler, so neither StaticEventHandler is ever registered and their WorldThingDied/NetworkProcess never fire (zero runtime AddEventHandler( calls exist either)
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/ui/vr_sdf_combos.zs:114 — SpawnSigil IS called here, but from SDFComboArbiter.WorldThingDied; since that handler is unregistered (common.txt:8), this call site is dead — the strongest potential refutation, and it fails
  3. E:/DoomXR-work/DOOM_FRESH/src/playsim/vr_msdf.cpp:253 — SpawnMSDFBillboard body is an explicit stub: '// TODO: Actually spawn the billboard actor here' + Printf only; no actor is ever spawned, and its only caller is the vr_debug_msdf test CCMD
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd_sdfmonster.zs:22 — GITD_SDFImp sets sprite=GetSpriteIndex("SIGL") (not blank-canvas) and has ZERO spawn/replaces/summon references anywhere; unzip -l of build/Release/doomxr.pk3 finds no SIGL/SDFG sprite lump, so gldefs SIGLA0/SDFGA0 bindings cannot attach
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/menudef.txt:3437 — SDFGifEntity is reachable only via 'netevent summon_gif_sentinel' dev menu command handled by the unregistered VRGifEventHandler, not any gameplay path

####################################################################################################
CLAIM ID: toolchain.1  |  CLUSTER: SDF/MSDF Toolchain & GIF Pipeline
CLAIM: SDF sidecar engine + MSDF conversion kit + JSON modder-forward customization + randomization + font support + customizable offsets + sequencing + reaction to game events/player behavior

INVESTIGATOR VERDICT: partial (conf high)

--- INVESTIGATOR REASONING ---
This claim is a bundle and splits sharply. REAL and wired: (1) the MSDF conversion kit — msdf-atlas-gen builds as a standalone exe (build/bin/Release/msdf-atlas-gen.exe) and font support exists via GITD's make_sdf_atlas.py (ttf/otf -> SDF atlas). (2) A shader-side procedural SDF 'sigil' system that IS fully end-to-end: SDFComboArbiter is a StaticEventHandler (auto-registers), its WorldThingDied fires on live kills, evaluates 21 keyword-driven combos, and calls VRSigilManager.SpawnSigil which reads real user cvars (color per hitzone, base complexity, randomize toggle — all present in the shipped CVARINFO), applies randomization, reacts to head/legs/hand/weapon/monster-type context, and spawns VRSigilGraphic feeding msdf_enabled/glitch/color into vr_sdf_procedural.fp (registered via gldefs.txt:11-14 as SIGLA0). That satisfies JSON-less customization + randomization + reaction to game events/player behavior. NOT working: the JSON/MSDF-atlas-fed C++ text system (FVRMSDFManager + FVRMSDFTextThinker). Its code is genuinely written and its call sites are live combat code (HEADSHOT/CRITICAL/damage numbers), but it is data-starved — vr_assets/msdf and vr_assets/json are empty and no 'Arcade' font atlas/json descriptor exists anywhere, so GetFontAsset returns null and SpawnText/SpawnImpact/RecordDamage all early-return; SpawnMSDFBillboard is an explicit TODO stub. I looked for but could not find: any glyph atlas asset, any .json font descriptor, any code that populates vr_assets, or an implemented body for SpawnMSDFBillboard. 'Customizable offsets' and 'sequencing' are only partially present (glyph Offset centering in RebuildGlyphs; per-style animation sequencing in Tick) and never fire due to the missing atlas. Net: partial — the toolkit and a procedural reactive path are real; the JSON/font-atlas customization pipeline is coded but unfed and stubbed at the billboard entrypoint.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/CMakeLists.txt:422-430 — MSDF_ATLAS_BUILD_STANDALONE=ON + add_subdirectory(libraries/msdf-atlas-gen); build/bin/Release/msdf-atlas-gen.exe exists = real MSDF conversion kit (a runnable tool)
  2. E:/DoomXR-work/DOOM_FRESH/src/d_main.cpp:3771 — FVRMSDFManager::GetInstance().Init() is actually called at startup; ScanLooseAssets parses JSON glyphs/metrics + _tribe.json color palettes/jitter/scale (vr_msdf.cpp:30-236) = real JSON customization/randomization code path
  3. E:/DoomXR-work/DOOM_FRESH/vr_assets/msdf and vr_assets/json are EMPTY, and no 'Arcade' atlas or planeBounds/atlasBounds .json exists anywhere in the tree — so GetFontAsset('Arcade') returns null and every SpawnText/RecordDamage in p_interaction.cpp:1674-1712 early-returns at runtime (data-starved)
  4. E:/DoomXR-work/DOOM_FRESH/src/playsim/vr_msdf.cpp:248-256 — SpawnMSDFBillboard body is a '// TODO: Actually spawn the billboard actor here' stub that only Printf's; the C++ billboard entrypoint is not implemented
  5. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/ui/vr_sdf_procedural.zs:97 + vr_sdf_combos.zs:12/62/114 — SDFComboArbiter StaticEventHandler.WorldThingDied -> VRSigilManager.SpawnSigil reads vr_sigil_* cvars (CVARINFO:14-18), randomizes hash/complexity, and reacts to kill context/hitzone/hand/weapon keywords -> VRSigilGraphic -> vr_sdf_procedural.fp = a SEPARATE, fully-wired shader-side SDF path

ADVERSARIAL: survives=True finalVerdict=partial (conf high)
--- REFUTATION ---
The original "partial (high)" verdict survives independent re-investigation. Both halves confirmed: the MSDF conversion kit + native shader-uniform plumbing + reactive StaticEventHandler combo/sigil logic are genuinely wired and run on live kill events; the JSON/atlas/font-fed C++ text system (FVRMSDFManager text, damage numbers, wired impacts) is coded on live call sites but data-starved (empty vr_assets, no 'Arcade' atlas) and its billboard entrypoint is a TODO stub. I additionally found the sigil path is weaker than the auditor stated — the required SIGLA0 sprite lump is absent everywhere and sdf_combos.json is never parsed (combos hardcoded) — which reinforces rather than overturns "partial."
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_sprites.cpp:114 — state.SetMSDFParams(actor->msdf_enabled, actor->msdf_glitch, actor->msdf_color) plus actor.h:1301-1303 native fields and vmthunks_actors.cpp:2064-2066 DEFINE_FIELD prove the shader-side SDF path (msdf_* ZScript field -> render state -> shader uniform) is fully wired end-to-end in native code
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/vr_msdf.cpp:248-256 — SpawnMSDFBillboard body is a '// TODO: Actually spawn the billboard actor here' that only Printf's; the C++ billboard entrypoint is a genuine stub
  3. E:/DoomXR-work/DOOM_FRESH/vr_assets/msdf + vr_assets/json — both empty and no 'Arcade' atlas / planeBounds json exists anywhere in tree, so GetFontAsset('Arcade') returns null and SpawnText/RecordDamage at p_interaction.cpp:1674-1712 early-return at runtime (data-starved)
  4. doomxr.pk3:gldefs.txt:11-13 binds HardwareShader Sprite SIGLA0 -> vr_sdf_procedural.fp and vr_sdf_procedural.zs:97 does GetSpriteIndex('SIGL'), but NO SIGL/SIGLA0 sprite lump exists in the pk3, wadsrc, or GITD repo — the sigil actor has no base sprite to render, a gap the original auditor missed
  5. doomxr.pk3:sdf_combos.json ships but grep for 'sdf_combos' across wadsrc/src/GITD finds only the #include of vr_sdf_combos.zs (a filename coincidence); SDFComboArbiter.OnRegister() hardcodes all combos in ZScript, so the JSON sidecar is never parsed = 'JSON modder-forward' combo customization is unwired

####################################################################################################
CLAIM ID: toolchain.2  |  CLUSTER: SDF/MSDF Toolchain & GIF Pipeline
CLAIM: Included tools for automated GIF-breakdown and SDF generation

INVESTIGATOR VERDICT: partial (conf high)

--- INVESTIGATOR REASONING ---
The claim has two halves with opposite outcomes. SDF generation: REAL — make_sdf_atlas.py is a working script that rasterizes any Windows .ttf/.otf into an SDF atlas via distance_transform_edt, and msdf-atlas-gen.exe builds as a standalone MSDF generator; both are usable tools. Automated GIF-breakdown: effectively ABSENT as a wired feature. giflib and EasyGifReader are vendored and DO compile into gif.lib/EasyGifReader.lib, but they are never added to PROJECT_LIBRARIES, so they are not linked into the zdoom executable, and I found zero #include or call sites (no gif_lib.h, DGifOpen, EasyGifReader) anywhere in src/ — nothing decodes a GIF. On the mod side, the only GIF-facing ZScript (sdf_gif_entity.zs, with the msdf_atlas.fp/SDFG atlas-frame shader whose header literally says 'MSDF Atlas Shader for Animated GIF Entities') is not #included in zscript.txt, so SDFGifEntity/VRGifEventHandler are never even parsed; and had they been, they carry a 'Test hardcode' frameCount and only spawn from a debug summon netevent, with no pipeline that turns a GIF into the frame atlas the shader indexes. I looked for but could not find: any linkage of the GIF libs into the game, any caller of the GIF decoder, an #include of sdf_gif_entity.zs, or any code that converts a .gif into frames/atlas. Net: partial — SDF generation tooling is real and present; the automated GIF-breakdown tooling is a compiled-but-unlinked, uncalled, un-included skeleton.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/CMakeLists.txt:419-420 — add_subdirectory(libraries/giflib) + add_subdirectory(libraries/EasyGifReader) compile the GIF libs (build/libraries/giflib/Release/gif.lib and EasyGifReader/Release/EasyGifReader.lib exist), but src/CMakeLists.txt:332-338 PROJECT_LIBRARIES lists miniz/webp/zvulkan/zwidget etc. and NEVER appends gif or EasyGifReader, so the linked target (zdoom, line 1410) does not include them
  2. grep of E:/DoomXR-work/DOOM_FRESH/src for gif_lib.h / DGifOpen / EasyGifReader returned ZERO matches — no engine source includes or calls any GIF library; the vendored GIF code is a compiled orphan with no consumer
  3. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/ui/sdf_gif_entity.zs defines SDFGifEntity + VRGifEventHandler (the only GIF-entity/frame-animation code), but zscript.txt does NOT #include it (includes at lines 19-23 cover vr_sdf_combos/vr_sdf_procedural but not sdf_gif_entity) = dead, never-compiled file
  4. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/ui/sdf_gif_entity.zs:53 — even inside that dead file, frameCount=16 is a '// Test hardcode' and it only spawns via a 'summon_gif_sentinel' debug netevent; no code loads a GIF, decodes frames, or builds the atlas the SDFG/msdf_atlas.fp shader would sample
  5. E:/_gitd35_repo/_fontsdf/make_sdf_atlas.py:1-62 (+ msdf-atlas-gen.exe) — a real, runnable SDF-generation tool: PIL/numpy/scipy bakes a ttf/otf into an SDF atlas (neonfont.png); this half of the claim is genuinely present

ADVERSARIAL: survives=True finalVerdict=partial (conf high)
--- REFUTATION ---
The original partial verdict survives independent re-investigation.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/CMakeLists.txt:419-420 — add_subdirectory(giflib)+(EasyGifReader) build gif.lib/EasyGifReader.lib, but src/CMakeLists.txt:332 PROJECT_LIBRARIES never appends them, so they are not linked into the zdoom target (link at src/CMakeLists.txt:1410)
  2. E:/DoomXR-work/DOOM_FRESH/src — grep for gif_lib.h/DGifOpen/EasyGifReader/EGifOpen/decodeAllFrames/readNextFrame returned ZERO matches: no C++ source includes or calls any GIF decoder; the vendored GIF libs are a compiled orphan with no consumer
  3. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript.txt — exhaustive search of all 326 #includes and every .zs/.txt in the pk3 found NO reference to sdf_gif_entity; SDFGifEntity/VRGifEventHandler are never parsed = dead file
  4. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3:zscript/ui/sdf_gif_entity.zs:61 — frameCount=16 is a '// Test hardcode', spawned only via the 'summon_gif_sentinel' debug netevent (line 46); no code loads a GIF, decodes frames, or builds the atlas msdf_atlas.fp samples
  5. E:/_gitd35_repo/_fontsdf/make_sdf_atlas.py:1-62 — real runnable SDF-generation tool (PIL/numpy/scipy distance_transform_edt bakes ttf/otf into neonfont.png, present 18KB); plus standalone build/bin/Release/msdf-atlas-gen.exe — this half of the claim is genuinely present

####################################################################################################
CLAIM ID: throw.3  |  CLUSTER: Throwing, Grenades & Throwables
CLAIM: Velocity-tracked throwing with a trajectory arc

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
Two independent, live implementations back this claim. (1) The C++ VR gravity-gloves path: VR_UpdateGravityGloves is invoked from P_PlayerThink at p_user.cpp:1436 (verified the compiled file is p_user.cpp, since only that file is in CMakeLists.txt:966 and the p_user_backup.cpp copy with its own call is excluded from the build). It reads real controller hand velocity, smooths it over a 4-sample rolling buffer, and on grip release applies that smoothed velocity (scaled by vr_throw_force, a genuine archived CVAR from hw_vrmodes.cpp:486, and by item mass) directly to the item's Vel — that is velocity-tracked throwing. While gripping a throwable it renders a 40-step predicted arc using the same velocity, stepping with gravity and doing Trace() collision, spawning particles along the arc. (2) The ZScript grenade path: GrenadeHandler.RenderOverlay draws a magenta parabolic arc with DrawThickLine, gated on the vr_grenade_trajectory CVAR (declared true in CVARINFO and read here) and on the player holding a HandGrenade, terminating on LineTrace collision. Both the velocity-tracked throw and the trajectory-arc visualization are wired end-to-end on live paths. The one caveat (which limits WHAT is throwable, covered under throw.1) is that the C++ velocity-throw only fires for MF_MISSILE items or actors tagged flags:throwable — but the throwing/arc machinery itself is fully functional.

--- INVESTIGATOR EVIDENCE ---
  1. src/playsim/p_user.cpp:1436 — VR_UpdateGravityGloves(player) is called every tic from P_PlayerThink (the live per-tic player thinker); p_user_backup.cpp is NOT in CMakeLists (only p_user.cpp at CMakeLists.txt:966) so it is dead
  2. src/playsim/p_user.cpp:1485-1494 — per-hand hand velocity is read via GetHandVelocity and smoothed with a 4-frame rolling-average buffer (vr_hand_vel_buffer)
  3. src/playsim/p_user.cpp:1506-1513 — on grip release, throwable item gets Vel = handVelocity * (vr_scale_meters_to_units/35.0) * throwForce where throwForce = vr_throw_force * massScale; vr_throw_force is a real CVAR (hw_vrmodes.cpp:486)
  4. src/playsim/p_user.cpp:1682-1709 — while gripping a throwable, a 40-step predicted trajectory arc is projected from handVelocity*velocityScale with gravity, doing per-step Trace() wall/floor collision and spawning arc particles
  5. wadsrc/static/zscript/actors/doom/weapongrenade.zs:188-228 — a second, independent grenade-arc renderer: GrenadeHandler.RenderOverlay draws a parabolic arc via Screen.DrawThickLine gated on the vr_grenade_trajectory CVAR (CVARINFO:5, =true) plus holding HandGrenade, with LineTrace collision termination

ADVERSARIAL: survives=True finalVerdict=working (conf high)
--- REFUTATION ---
The claim survives. The C++ velocity-tracked throw with trajectory arc is fully wired end-to-end on a live per-tic path, and all cited CVARs/methods are real (not stubs). One caveat: the auditor's secondary evidence, the ZScript GrenadeHandler.RenderOverlay arc, is unwired dead code (GrenadeHandler is not registered in AddEventHandlers and has no Register() call), so it never renders — but the capability is independently satisfied by the C++ implementation.
 --- ADVERSARY EVIDENCE ---
  1. src/playsim/p_user.cpp:1436 — VR_UpdateGravityGloves(player) called unconditionally inside P_PlayerThink (defined 1347, invoked on live thinker at 2207); only p_user.cpp is in CMakeLists.txt:966, backup excluded — live per-tic path confirmed
  2. src/common/rendering/vulkan/stereo3d/vk_openxrdevice.cpp:4355 + :3262 — GetHandVelocity is a REAL override averaging an OpenXR linearVelocity history buffer written every frame; base class returns false but the actual VR device implements it (not a stub)
  3. src/playsim/p_user.cpp:1506-1513 — on grip release heldItem->Vel = smoothed handVelocity * (vr_scale_meters_to_units/35)*vr_throw_force*massScale; both CVARs real (hw_vrmodes.cpp:486=1.0, :388=40.0) — velocity-tracked throw is wired
  4. src/playsim/p_user.cpp:1682-1716 — while gripping a throwable, live 40-step gravity-stepped predicted arc with Trace() collision and P_SpawnParticle rendering — the arc visualization is wired via C++
  5. wadsrc/static/mapinfo/common.txt:8 + src/events.cpp:590 — GrenadeHandler (StaticEventHandler, weapongrenade.zs:128) is ABSENT from AddEventHandlers and has no Register() call; InitStaticHandlers only instantiates handlers named in EventHandlers lists, so GrenadeHandler.RenderOverlay (the auditor's 2nd arc) is dead code

####################################################################################################
CLAIM ID: throw.2  |  CLUSTER: Throwing, Grenades & Throwables
CLAIM: Universal throwable grenade with physics-based explosion

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
End-to-end trace is complete and live. Key g fires the throw_grenade netevent (keyconf.txt, verified present in the shipped pk3), which is consumed by GrenadeHandler.NetworkProcess — a StaticEventHandler subclass that auto-registers, no AddEventHandlers entry needed. It spawns ThrownGrenade, a genuine gravity+bounce physics projectile whose Tick() fuse triggers Explode() with real A_Explode radius damage plus shrapnel and an FX actor. GrenadeAmmo and the HandGrenade weapon exist and are distributed. 'Universal' is slightly generous (it is a dedicated grenade item, not any object), but a physics-based throwable grenade with a physics explosion is fully wired and runs on a live path. Note: the HandGrenade weapon's own Fire/Hold/Throw states never Goto Throw (Hold loops on A_ReFire), so the weapon-state throw path is unreachable, but the netevent path is the live one and works, so this does not downgrade the verdict.

--- INVESTIGATOR EVIDENCE ---
  1. wadsrc/static/keyconf.txt:5-6 — alias throwgrenade = 'netevent throw_grenade' with defaultbind g; confirmed baked into build/Release/doomxr.pk3 keyconf.txt (auto-loaded KEYCONF lump)
  2. wadsrc/static/zscript/actors/doom/weapongrenade.zs:128-149 — GrenadeHandler:StaticEventHandler.NetworkProcess catches 'throw_grenade', checks/decrements GrenadeAmmo, plays grntoss, and SpawnPlayerMissile('ThrownGrenade')
  3. wadsrc/static/zscript/actors/doom/weapongrenade.zs:54-96 — ThrownGrenade is a real physics projectile: Gravity 0.8, +BOUNCEONFLOORS/WALLS/CEILINGS, BounceFactor, OnBounce sound, and a 105-tic fuse timer in Tick() that calls Explode()
  4. wadsrc/static/zscript/actors/doom/weapongrenade.zs:98-109 — Explode() runs double A_Explode(160,256)+A_Explode(120,128) radius damage, spawns GrenadeExplosionEffect and 16 GrenadeShrapnel projectiles, then Destroy()
  5. wadsrc/static/zscript/engine/vr_weapon_logic.zs:44,57 — replacement/give logic distributes HandGrenade+GrenadeAmmo, and doomplayer.zs:25 assigns Player.WeaponSlot 8 'HandGrenade'

ADVERSARIAL: survives=False finalVerdict=partial (conf high)
--- REFUTATION ---
GrenadeHandler and WeaponReplacementHandler are never registered; only path is dead.
 --- ADVERSARY EVIDENCE ---
  1. test

####################################################################################################
CLAIM ID: throw.1  |  CLUSTER: Throwing, Grenades & Throwables
CLAIM: Throwable barrels, corpses, and weapons

INVESTIGATOR VERDICT: partial (conf high)

--- INVESTIGATOR REASONING ---
The keyword-throwable pipeline itself is genuinely wired: KEYWORDS.json defines a flags:throwable profile, KeywordDispatcher::IsThrowable() parses it (keyword_dispatcher.cpp:275) and IS called on a live path (p_user.cpp:1504 and 1685 inside VR_UpdateGravityGloves, which runs every tic from P_PlayerThink), where its boolean branches throw-vs-drop. But the claim's specific subjects fail. Barrels: ExplosiveBarrel is +SOLID +SHOOTABLE (not MF_SPECIAL), so it fails the grab-candidate filter at p_user.cpp:1573 and can never be grabbed; its 'grab'/'class:barrel' keywords are inert (I searched keyword_dispatcher.cpp for the bare 'grab' token and found zero consumers). Corpses/monsters: same story — all monster actors carry 'grab'+'mass:N' but are MF_SHOOTABLE not MF_SPECIAL, so they are never grab candidates and the keywords are never consumed by the grab path. Weapons: they ARE MF_SPECIAL (grabbable) but carry only 'grab', not flags:throwable, so on release IsThrowable() returns false and they drop straight down rather than being thrown. The only non-missile actor actually flagged throwable is a test dummy that itself can't pass the grab filter. In practice only MF_MISSILE projectiles are grab-and-throwable. So a real, wired throw mechanism exists but does NOT cover barrels, corpses, or weapons as claimed — hence partial. Separately, a distinct inventory-based 'throw_weapon' netevent (key k) exists (see throw.3 chain), but that is not physics-grabbing the weapon actor.

--- INVESTIGATOR EVIDENCE ---
  1. src/playsim/p_user.cpp:1573 — grab-candidate filter only accepts MF_SPECIAL (pickups) or MF_MISSILE (projectiles); monsters/corpses and barrels are MF_SHOOTABLE not MF_SPECIAL, so they can never become grab candidates
  2. src/playsim/p_user.cpp:1504,1685 — throwable test is (heldItem->flags & MF_MISSILE) OR KeywordDispatcher::IsThrowable(heldItem->Keywords); non-throwable held items get Vel.Zero() (drop straight down, p_user.cpp:1518)
  3. wadsrc/static/zscript/actors/doom/doommisc.zs:10-19 — ExplosiveBarrel is +SOLID +SHOOTABLE (not MF_SPECIAL); its Keywords are only 'grab','class:barrel' with no flags:throwable, and 'grab' is never consumed in C++ (zero hits in keyword_dispatcher.cpp)
  4. wadsrc/static/zscript/actors/doom/weaponshotgun.zs:18 (all weapon+monster actors) — carry 'grab','mass:N' but NOT flags:throwable; weapons are MF_SPECIAL so grabbable, but on release IsThrowable()==false so they drop, not thrown
  5. wadsrc/static/zscript/actors/shared/vr_testing.zs:28 — the ONLY non-missile actor tagged Keywords 'flags:throwable' is the VRTestingGrabbable test dummy, and it is +SOLID +PUSHABLE (not MF_SPECIAL) so it also fails the p_user.cpp:1573 grab filter

ADVERSARIAL: survives=False finalVerdict=declared_unwired (conf high)
--- REFUTATION ---
Overturns 'partial' as too generous: the keyword-throwable pipeline is wired but NO named subject works. Gate A (grab admission, p_user.cpp:1573) only admits MF_SPECIAL|MF_MISSILE. Gate B (throw, p_user.cpp:1503) needs flags:throwable/flags:grenade profile keys. Barrels (+SOLID+SHOOTABLE) & corpses (MF_CORPSE) fail Gate A -> ungrabbable. Weapons are MF_SPECIAL so grabbable but carry no flags:throwable token -> Vel.Zero() drops them straight down. The ONLY actor flagged flags:throwable is VRTestingGrabbable (a debug dummy) and it's +SOLID+PUSHABLE so it can't pass Gate A either. The 'grab' keyword smeared on actors is decorative, never read in C++. Only real throwing = mid-air projectile catching (separate capability).
 --- ADVERSARY EVIDENCE ---
  1. p_user.cpp:1573 — grab filter admits only MF_SPECIAL|MF_MISSILE; barrels/corpses/monsters excluded
  2. p_user.cpp:1503-1518 — non-throwable held items get Vel.Zero() (drop); weapons carry no flags:throwable token
  3. vr_testing.zs:19-28 — the sole flags:throwable actor is +SOLID+PUSHABLE, ungrabbable; KEYWORDS.json:72-75 only grenade/throwable are throwable

####################################################################################################
CLAIM ID: hands.1  |  CLUSTER: VR Hands & Poses
CLAIM: Engine-coded IQM VR hands with programmed poses and reactions

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
Fully wired end-to-end. The IQM asset is real (valid 378KB InterQuake model with correct magic bytes; the empty hand.iqm is unused). modeldef.txt binds it to the VRHandModel ZScript class with 4 named pose frames (Idle/Grip/Climb/Point). Two independent live C++ render paths consume it: (1) the 'Draw VR Hands' block in hw_weapon.cpp and (2) the fist hot-swap in RenderHUDModel/models.cpp. Both read player->vr_hand_state and call FindModelFrame on the loaded VRHandModel to pick the IQM frame; both are gated on IsVR() and vr_show_hands (CVAR default true, declared hw_vrmodes.cpp:485, read at hw_weapon.cpp:925). The pose index is written every tic on the live P_PlayerThink path (p_user.cpp:1439-1449). 'Reactions' are real: VR_HapticEvent fires on climb texture (p_user.cpp:1946) and on grab. The fist-keyword match uses FString::IndexOf substring search so both Doom 'class:fist' and Hexen 'fist' keywords trigger the swap. I looked for but did not find any missing link: no dead definitions, no stubbed FindModelFrame, no unregistered path, and the content ships in doomxr.pk3.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/models/vhand.iqm:1 — 378548-byte file with magic 'INTERQUAKEMODEL' header proves a real, non-empty IQM hand model exists (sibling hand.iqm is 0 bytes/unused)
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/modeldef.txt:1-12 — 'Model VRHandModel' binds vhand.iqm + vhand.png and maps FrameIndex VHAN A/B/C/D to IQM frames 0-3 (the 4 poses), plus fist-sprite remapping (PUNG/FPCH)
  3. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_weapon.cpp:944-976 — live 'Draw VR Hands' render path (gated on vrmode->IsVR() && vr_show_hands) reads players[...].vr_hand_state[hand], maps 0/1/2/3 to Spawn/Grip/Climb/Point, and calls FindModelFrame(handClass,...) to select the IQM frame
  4. E:/DoomXR-work/DOOM_FRESH/src/r_data/models.cpp:214-241 — RenderHUDModel intercepts any weapon with 'fist' keyword, looks up VRHandModel, maps vr_hand_state to Grip/Climb/Point states, and swaps smf to the hand IQM frame (hot-swap wiring)
  5. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3 — unzip -l shows models/vhand.iqm, models/vhand.png, modeldef.txt and zscript/actors/shared/vr_hand.zs are all baked into the shipping build, not just source

ADVERSARIAL: survives=True finalVerdict=working (conf high)
--- REFUTATION ---
I independently re-opened every link in the chain and tried to break it, and it held. Asset: vhand.iqm is a real 378548-byte model with valid INTERQUAKEMODEL magic bytes (hand.iqm is 0 bytes/unused), and all four assets (iqm, png, vr_hand.zs, modeldef.txt) are baked into the shipping doomxr.pk3 (46MB, dated today). Binding: modeldef.txt maps VHAN A/B/C/D to IQM frames 0-3 on class VRHandModel. ZScript: vr_hand.zs defines `class VRHandModel : Actor` with exactly the Spawn/Grip/Climb/Point states the C++ FindState() queries, and it is #include'd at zscript.txt:94 so PClass::FindActor resolves it. Write path (the poses): vr_hand_state[hand] is written EVERY TIC inside P_PlayerThink (p_user.cpp:1439-1449), driven by real VR climb/grab/two-hand state (VR_UpdateClimbing/VR_UpdateGravityGloves/VR_CalculateTwoHanding are all real defined functions, not stubs); P_PlayerThink is invoked from the master tic loop at p_tick.cpp:172 and p_user.cpp is the compiled file per src/CMakeLists.txt:966 (p_user_backup.cpp is NOT in the build). Render: two independent live paths (models.cpp:214-241 fist hot-swap and hw_weapon.cpp:924-992 idle overlay) both read vr_hand_state, map to Grip/Climb/Point, and call FindModelFrame to select the IQM frame; both gated on IsVR() && vr_show_hands, and vr_show_hands defaults true (hw_vrmodes.cpp:485). Reactions are real: VR_HapticEvent fires on climb_texture (p_user.cpp:1946), grip_climb (1925), and grab snatch/deflect (1771/1838/1845). The only weakness I found is that no VHANA0 sprite lump exists in the pk3, so hw_weapon.cpp:948 CheckForTexture could make the idle-overlay path 'continue' out — but this does NOT sink the claim: the fist hot-swap path (bare-hands, the core mechanism) is sprite-independent and drives off real PUNG/FPCH psprites, and model frames resolve from the modeldef binding, not from a 2D sprite graphic. Verdict survives as working.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1439-1449 — vr_hand_state[hand] is written every tic inside P_PlayerThink from real VR climb/grip/two-hand booleans (0=Idle,1=Grip,2=Climb), proving poses are live-driven not hardcoded
  2. E:/DoomXR-work/DOOM_FRESH/src/p_tick.cpp:172 — P_PlayerThink is called from the master per-player tic loop, confirming the pose-write path runs on a live game tic
  3. E:/DoomXR-work/DOOM_FRESH/src/r_data/models.cpp:214-241 — RenderHUDModel intercepts any 'fist'-keyword psprite in VR, reads vr_hand_state, maps to Grip/Climb/Point, and swaps smf via FindModelFrame(VRHandModel...) — a sprite-lump-independent render consumer
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/shared/vr_hand.zs:2-25 — class VRHandModel:Actor defines Spawn/Grip/Climb/Point states (VHAN A/B/C/D); #include'd at zscript.txt:94 so PClass::FindActor resolves, and modeldef.txt:9-12 binds VHAN A-D to IQM frames 0-3
  5. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrmodes.cpp:485 — CVAR(Bool, vr_show_hands, true, ...) defaults ON and is read at hw_weapon.cpp:925, so the render gate is open by default

####################################################################################################
CLAIM ID: hands.2  |  CLUSTER: VR Hands & Poses
CLAIM: VR hands naturally adapt their pose to context (climbing, gravity-gloving, two-handing a scattergun)

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
The exact three contexts in the claim are all implemented and drive the hand pose on a live path. p_user.cpp:1434-1449 is the single decision point, executed inside P_PlayerThink after the three updater functions run (1435-1437). Each updater is substantive real logic operating on real VR inputs: climbing uses grip buttons, climbable-line keywords (KeywordDispatcher::IsClimbable), and hand velocity (GetHandVelocity), and it toggles NOGRAVITY + sets velocity; gravity-gloving grip-locks a candidate then magnet-pulls it toward the hand; two-handing measures hand-transform distance against a keyword-driven radius. All state vars (vr_is_climbing, vr_held_items, vr_grab_is_locked/pulling, vr_two_hand_stabilized) have real writers and are then read by the pose ladder. The one caveat: 'a scattergun' is not special-cased — two-handing is generic (any weapon passing the vr_twohand_whitelist keyword check), and the two-handed pose is simply the offhand switching to the same Grip frame; there is no dedicated longarm-grip pose distinct from the generic grip. So the mechanism the claim describes is fully wired, but the specific scattergun example is representative rather than a bespoke pose. I also noted the bullet-snatching sub-feature is commented out ('Tabled for now', p_user.cpp:1799-1839), but that is unrelated to the three claimed contexts.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1434-1449 — inside P_PlayerThink, an explicit priority ladder (comment 'Grip Priority: Climb > Gravity Gloves > Two-Handing') sets vr_hand_state[hand]: climbing->2(Climb), held/grab-locked/pulling->1(Grip), offhand while two-hand-stabilized->1(Grip), else 0(Idle)
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1435-1437 — VR_UpdateClimbing / VR_UpdateGravityGloves / VR_CalculateTwoHanding are all CALLED each tic immediately before the pose ladder, so the inputs are freshly computed on the live path
  3. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1959-1971 — VR_UpdateClimbing sets vr_is_climbing[hand] from grip-press + keyword-climbable line, then applies MF_NOGRAVITY and climb velocity: real climbing logic, not a stub
  4. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1791-1797 — VR_UpdateGravityGloves pulls a grab candidate toward the hand (target->Vel = diff * vr_grab_magnet_speed) and sets vr_grab_is_pulling/vr_held_items, feeding the Grip pose
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1305-1332 — VR_CalculateTwoHanding reads main/offhand transforms + weapon keyword offsets (KeywordDispatcher::GetWeaponOffsets) and sets vr_two_hand_stabilized when hands are within radius, which flips the offhand to Grip

ADVERSARIAL: survives=True finalVerdict=working (conf high)
--- REFUTATION ---
The verdict survives. Every link in the chain is wired on a live per-tic path with real writers and two acting consumers.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1435-1449 — three updaters run then the pose ladder sets vr_hand_state[hand] to Climb(2)/Grip(1)/Grip-offhand(1)/Idle(0); block is inside P_PlayerThink (opens 1347)
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:2207 — P_PlayerThink(player) is called each tic in the prediction/ticker loop, so the pose ladder runs on a live path
  3. E:/DoomXR-work/DOOM_FRESH/src/r_data/models.cpp:222-240 — RenderHUDModel reads vr_hand_state[handIdx] and swaps the hand model to the Grip/Climb/Point ZScript state: a real consumer acting on the value
  4. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_weapon.cpp:944-969 — hand sprite (VHANA0..VHAND0) and state name are selected from vr_hand_state[hand]: a second independent consumer
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_user.cpp:1963-1970 — VR_UpdateClimbing writes vr_is_climbing[hand] from grip+climbable keyword, then sets MF_NOGRAVITY and climb velocity (CMakeLists.txt:966 compiles this file, not p_user_backup.cpp)

####################################################################################################
CLAIM ID: voxel.1  |  CLUSTER: Voxels
CLAIM: Baked-in voxels for vanilla pickups, weapons, and decorations

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
Traced end to end and confirmed working. The 188 .kvx files are baked into the built doomxr.pk3 under voxels/ (verified via unzip -l, not just the source tree). The filesystem loader tags anything under voxels/ as ns_voxels (filesystem.cpp:139), the sprite init loop scans exactly that namespace and binds voxels to vanilla sprite frames by name (sprites.cpp:350-352), and R_InitVoxels parses the shipped voxeldef.txt->VOXELDEF lump for per-voxel spin/scale/angle options (voxeldef.cpp:272-314). The whole chain is reached from R_InitSprites, called from live level setup p_setup.cpp:650. This is the stock GZDoom Reikall/Doom-3D voxel mechanism, but it is fully wired and the assets are present, so vanilla pickups/weapons/decorations render as voxels. I found no evidence the voxels dir is excluded from packaging or that the bind path is gated off; the only global gate is r_drawvoxels which defaults true (r_utility.cpp:110).

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3 — `unzip -l` shows 188 baked `.kvx` voxel entries under `voxels/` (plus 9 under `voxels/64/`) covering vanilla pickups/weapons/decorations (AMMOA, CLIPA, PISTA, MGUNA, LAUNA, PLASA, BFUGA, BKEYA, MEDIa, ARM1A, COL5A, etc.), so the assets are shipped in the build, not just loose in source
  2. E:/DoomXR-work/DOOM_FRESH/src/common/filesystem/source/filesystem.cpp:139 — `!strncmp(LongName, "voxels/", 7) ? ns_voxels` assigns every file under `voxels/` to the ns_voxels namespace that the sprite loader scans for voxel replacements
  3. E:/DoomXR-work/DOOM_FRESH/src/r_data/sprites.cpp:350-352 — the voxel auto-bind loop iterates all filesystem entries, gates on `GetFileNamespace(i) == FileSys::ns_voxels`, and binds each .kvx to a sprite frame by short name; live consumer that replaces vanilla sprites with voxels
  4. E:/DoomXR-work/DOOM_FRESH/src/r_data/sprites.cpp:1001-1002 — inside `R_InitSprites()`, `R_InitSpriteDefs()` (voxel bind) then `R_InitVoxels()` (VOXELDEF parse) both run; R_InitSprites is invoked from p_setup.cpp:650 on every map load, a live path
  5. E:/DoomXR-work/DOOM_FRESH/src/r_data/voxeldef.cpp:272,285,309 — `R_InitVoxels()` does `FindLump("VOXELDEF")` and `CheckNumForFullName(..., ns_voxels)`; the shipped `voxeldef.txt` (in pk3, 3619 bytes) resolves as the VOXELDEF lump and its spin/scale/angle/droppedspin options are consumed by real branching that builds FVoxelDef objects

ADVERSARIAL: survives=True finalVerdict=working (conf high)
--- REFUTATION ---
I attacked the "working" verdict by hunting for the missing link that would make baked-in voxels NOT render: an absent render-time consumer, a default-off gate, a stub loader, or a packaging exclusion. I found none — every link is live. I opened each cited file myself. filesystem.cpp:139 does map voxels/ -> ns_voxels (not a comment). sprites.cpp:350-460 is a real loop that scans ns_voxels, parses the sprite name, calls R_LoadVoxelDef, and writes the result into sprtemp[j].Voxel -> installed into SpriteFrames. I confirmed R_LoadVoxelDef (voxels.cpp:307) is not a stub: it calls R_LoadKVX(lumpnum), builds an FVoxelDef, and pushes it. I traced the startup path: d_main.cpp:3864 P_Init() -> p_setup.cpp:650 R_InitSprites() -> R_InitSpriteDefs()+R_InitVoxels() (sprites.cpp:1001-1002), a real init reached at engine startup (their "every map load" phrasing is slightly off — it is startup-once via P_Init, not per-map — but it is still a live path). Crucially I found the RENDER-TIME consumer the original auditor did not cite: models.cpp:1201-1213, the hardware (GL/Vulkan) model-frame lookup, reads sprframe->Voxel exactly where the bind loop wrote it, gated only by r_drawvoxels, and returns the voxel model frame in place of the vanilla sprite. That gate defaults true (r_utility.cpp:110 `CVAR (Bool, r_drawvoxels, true, 0)`). The 188 .kvx assets and the 3619-byte voxeldef.txt are baked into the built doomxr.pk3 (verified with unzip -l, not just source). Every declaration has a real caller/consumer on a live path. The verdict survives.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3 — unzip -l shows 188 .kvx entries under voxels/ (AMMOA, BFUGA, BKEYA, ARM1A, BON1A, etc.) plus voxeldef.txt (3619 bytes) baked into the shipped build, not just loose source
  2. E:/DoomXR-work/DOOM_FRESH/src/r_data/sprites.cpp:352,391-397,435-453 — live bind loop scans ns_voxels, hashes each .kvx by 4-char sprite name, calls R_LoadVoxelDef, and writes the FVoxelDef into sprtemp[j].Voxel for the matching vanilla sprite frame
  3. E:/DoomXR-work/DOOM_FRESH/src/common/models/voxels.cpp:307-327 — R_LoadVoxelDef is not a stub: it calls R_LoadKVX(lumpnum), builds/populates an FVoxelDef (Scale, Spin, AngleOffset) and pushes it; returns it to the bind loop
  4. E:/DoomXR-work/DOOM_FRESH/src/r_data/models.cpp:1202-1213 — the render-time model-frame lookup reads sprframe->Voxel (populated by the bind loop), gated on r_drawvoxels, and returns the voxel model frame in place of the vanilla sprite — the live consumer that makes pickups/decorations actually render as voxels
  5. E:/DoomXR-work/DOOM_FRESH/src/d_main.cpp:3864 -> src/p_setup.cpp:650 -> src/r_data/sprites.cpp:1001-1002 & src/rendering/r_utility.cpp:110 — P_Init()->R_InitSprites()->R_InitSpriteDefs()/R_InitVoxels() runs at startup on a live path, and the only global gate r_drawvoxels defaults true

####################################################################################################
CLAIM ID: voxel.2  |  CLUSTER: Voxels
CLAIM: Real-time voxel culling in a radius around the player

INVESTIGATOR VERDICT: absent (conf high)

--- INVESTIGATOR REASONING ---
No implementation of radius-based voxel culling exists anywhere. I searched the C++ engine (src/), the ZScript (wadsrc/static/zscript), the GITD mod repo (E:/_gitd35_repo), menudef/cvarinfo, and the built pk3. The only voxel toggle is stock global r_drawvoxels (bool, no radius). What superficially resembles proximity logic is unrelated: models_voxel.cpp backfacecull is per-face geometry culling in the KVX data; gl_IsDistanceCulled applies to dynamic lights not voxels; sv_wallcheck + A_CheckProximity is wall-collision-based auto-rotation of decoration voxels; and vr_voxel_assembly.zs is a cosmetic equip particle effect. I specifically looked for a per-frame EventHandler, a distance/maxdist cvar read by voxel draw logic, or a proximity gate in the hardware sprite/voxel render path, and found none. Verdict absent (no implementation), high confidence.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src — grep for `voxel.*(cull|radius|distance|maxdist)` and `(cull|proximity|nearplayer|distancecull).*vox` returned zero hits; no engine code correlates voxel rendering/removal with player distance or radius
  2. E:/DoomXR-work/DOOM_FRESH/src/rendering/r_utility.cpp:110 — the ONLY voxel cvar is stock `CVAR(Bool, r_drawvoxels, true, 0)`, a global on/off with no radius; its consumers (d_main.cpp:783 sets RFF_VOXELS, models.cpp:1202, r_opaque_pass.cpp:1127) are unconditional feature toggles, not radius culls
  3. E:/DoomXR-work/DOOM_FRESH/src/common/models/models_voxel.cpp:255 — `int cull = voxptr->backfacecull` is per-face KVX geometry backface culling baked into the voxel data, unrelated to player proximity
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/vox_decorate.txt:76 + vox_menudef.txt:10 — the only custom voxel-adjacent runtime cvar `sv_wallcheck` drives A_SpawnItemEX/A_CheckProximity WALL-avoidance auto-rotation of decorations (facing them away from walls), not player-radius culling of voxels
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/effects/vr_voxel_assembly.zs:1-60 — the only DoomXR ZScript containing 'voxel' is a cosmetic VoxelParticle weapon-equip effect (A_DataSiphonEquip, wired to 4 weapons) spawning particles that fly toward the player; performs no voxel culling

ADVERSARIAL: survives=False finalVerdict=partial (conf medium)
--- REFUTATION ---
See above.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_sprites.cpp:852-862 — IsDistanceCulled(AActor*) computes (thing->Pos()-r_viewpoint.Pos).LengthSquared() and returns true beyond gl_sprite_distance_cull^2; a real player-radius cull the prior auditor missed
  2. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_sprites.cpp:850 — CVAR(Float, gl_sprite_distance_cull, 2000.0,...): the radius; hw_sprites.cpp:873 calls IsDistanceCulled(thing) as the first line of HWSprite::Process, before any voxel/model/sprite branch
  3. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_bsp.cpp:933,937,964,968 — RenderThings loops touching_renderthings and calls sprite.Process for EVERY thing (incl. isVoxel model frames, hw_sprites.cpp:976) with no voxel gate, so voxels pass through the distance cull
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/menudef.txt:3837 — the cull is stock GZDoom exposed as $DSPLYMNU_SPRITECULL slider on gl_sprite_distance_cull (general sprite/thing cull), not a DoomXR voxel-specific feature
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/effects/vr_voxel_assembly.zs:20-31 — the only DoomXR voxel ZScript ATTRACTS particles toward the player (vel = dir.Unit()*12), performing zero culling; confirms no bespoke voxel-radius culler exists

####################################################################################################
CLAIM ID: weapons.2  |  CLUSTER: Weapons System (universal model set, dual-wield, assignment)
CLAIM: Dual-wield support

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
Dual-wield/offhand is a deeply and consistently wired system spanning both the C++ playsim (OffhandWeapon field, PSP_OFFHANDWEAPON layer, offhand-aware attack/aim/use code in p_map/p_mobj/p_effect/p_hitscantracer) and ZScript (53 files reference OffhandWeapon/bOffhandWeapon/ALF_ISOFFHAND, including weapons.zs weapon-give/ready logic and the player MoveWeaponToHand virtual actually called by the VR weapon wheel). This is the QuestZDoom/GZ3Doom VR lineage's offhand system carried into this fork and it is exercised end-to-end: a weapon can be assigned to the offhand, gets its own psprite layer, and fires as a real attack. The four new weapons integrate via their bOffhandWeapon-aware fire helpers. I found no missing call site or stub in the core path. (Note: DualPistol.md3/Dualplasma.md3 model assets exist but are unreferenced — dead art, not part of the wired mechanism.)

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/playsim/d_player.h:384 — player_t carries a real OffhandWeapon AActor* field, GC-marked (p_user.cpp:409) and copied on state restore (p_user.cpp:334).
  2. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_pspr.h:59 + p_pspr.cpp:329-331,545,675 — PSP_OFFHANDWEAPON is a live psprite layer whose caller resolves to OffhandWeapon and which is ticked/animated independently of the main hand.
  3. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_map.cpp:4579,4826,7515 + p_mobj.cpp:7508 + p_effect.cpp:727 — attack/hitscan/rail code branches on ALF_ISOFFHAND/LAF_ISOFFHAND to fire from OffhandWeapon vs ReadyWeapon, so offhand shots are real attacks, not cosmetic.
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/player/player.zs:2528 — MoveWeaponToHand(Weapon,int hand) virtual exists and is invoked from the VR wheel C++ (hw_vrwheel.cpp:133-145,810) to assign a weapon to the offhand, setting player.OffhandWeapon (weapons.zs:919,932,965).
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/engine/vr_weapon_helpers.zs:183-222 — the new weapons' A_BallisticFire computes hand from weap.bOffhandWeapon and passes ALF_ISOFFHAND, so the added VR weapons participate in offhand firing.

ADVERSARIAL: survives=True finalVerdict=working (conf high)
--- REFUTATION ---
I independently re-traced the entire dual-wield chain from user input to real attack and could not break the verdict. INPUT: menudef.txt:918/956/3176 binds a physical control to +vroffhandweaponwheel; that command is registered as a live CCMD (g_game.cpp:453-456) which calls VRWheel_OpenOffhandWeapon (g_game.cpp:425). WHEEL COMMIT: hw_vrwheel.cpp:804-810 sets targetOffhand=true for EVRWheelType::OffhandWeapon and invokes the ZScript virtual MoveWeaponToHand, which really sets weap.bOffhandWeapon and player.PendingWeapon (player.zs:2554-2559). BRING-UP/TICK: p_pspr.cpp:1450-1454 spawns the offhand weapon via a second P_BringUpWeapon; the PSP_OFFHANDWEAPON layer is created (p_pspr.cpp:343) and lives in the shared psprites linked list ticked by NewTick (p_pspr.cpp:475+). REAL ATTACK: p_map.cpp:4600-4606 aims hitscans from the VR OffhandPos/OffhandDir and 4826 resolves the source to player->OffhandWeapon when ALF_ISOFFHAND is set, so offhand shots are genuine independently-aimed attacks. NEW WEAPONS: vr_weapon_helpers.zs:186,208,222 computes hand from weap.bOffhandWeapon and passes ALF_ISOFFHAND. I searched for a default-off cvar gate, a stub, an unregistered command, or a dead branch and found none. The only dead element is unreferenced DualPistol/Dualplasma model art, which is not part of the wired mechanism.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/menudef.txt:918 — binds a real VR control to +vroffhandweaponwheel, proving the offhand wheel is user-reachable, not orphaned code
  2. E:/DoomXR-work/DOOM_FRESH/src/g_game.cpp:455 — C_RegisterFunction("+vroffhandweaponwheel", ..., Cmd_VROffhandWeaponWheelDown) registers the live CCMD that opens the offhand wheel (calls VRWheel_OpenOffhandWeapon at g_game.cpp:425)
  3. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_vrwheel.cpp:809 — targetOffhand = (Type==OffhandWeapon) then MoveWeaponToHand(...), assigning the picked weapon to player.OffhandWeapon via the ZScript virtual (player.zs:2554)
  4. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_pspr.cpp:1450 — P_BringUpWeapon is called a second time for player->OffhandWeapon, so the offhand weapon gets its own PSP_OFFHANDWEAPON psprite layer that is ticked each frame
  5. E:/DoomXR-work/DOOM_FRESH/src/playsim/p_map.cpp:4826 — attack code selects player->OffhandWeapon and aims from OffhandPos/OffhandDir (4600-4606) when ALF_ISOFFHAND is set, proving offhand shots are real independently-aimed attacks

####################################################################################################
CLAIM ID: weapons.1  |  CLUSTER: Weapons System (universal model set, dual-wield, assignment)
CLAIM: Universal model set for default weapons for universal VR compatibility with loaded pk3s (revolver, rifle, smg, flamethrower added)

INVESTIGATOR VERDICT: partial (conf high)

--- INVESTIGATOR REASONING ---
The four VR weapons exist as real, fully-stated actors and their md3 model files exist on disk. A genuine 'universal model set' render path IS wired end-to-end: FVRWeaponResolver::Init runs at startup, auto-assigns archetypes by inheritance, and hw_weapon.cpp + p_pspr.cpp consume the archetype to inject the archetype actor's 3D model over ANY loaded weapon sprite (the pk3-compat mechanism), gated by vr_weapon_shell (default true). However it is PARTIAL for three concrete reasons I verified: (1) the flamethrower's modeldef binding is broken — actor sprite HBFT vs modeldef FLMG; (2) the four new weapons inherit plain DoomWeapon so their own archetype auto-resolves to Unknown, so the shell system does not drive their models (they rely solely on direct modeldef, which works for 3 of 4); (3) the advertised vr_spawn_models 'Spawn 3D Models instead of Sprites' toggle is a dead stub in both A_CheckSpawnModel and the shotgun Spawn block — I looked for its consumer and found only comment bodies. So the capability is real and wired for most weapons but has a broken flamethrower mapping and a non-functional advertised toggle.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/d_main.cpp:3770 — FVRWeaponResolver::Init() is called on the live engine-startup path (right after PClassActor::StaticInit), so the archetype resolver runs every launch.
  2. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_weapon.cpp:1684-1826 — the renderer reads ReadyWeapon->vr_weapon_data->Archetype, calls GetActorClassForArchetype, and swaps in that archetype actor's model frame at draw time (gated by vr_weapon_shell). This is the real universal-VR-model mechanism for foreign pk3 weapons that inherit base Doom classes.
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/modeldef.txt:100-160 — VR_Rifle/VR_Revolver/VR_SMG/VR_Flamethrower map sprites to md3 models via FrameIndex; RIFL/REVL/SMGG match their actors, but VR_Flamethrower maps FLMG while the Flamethrower actor uses sprite HBFT (weaponflame.zs) — a mismatch so the flamethrower model never binds via modeldef.
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/doom/doomweapons.zs:7 — Revolver/Rifle/SMG/Flamethrower all inherit DoomWeapon:Weapon, not Shotgun/Pistol/etc, so ResolveWeapons() auto-detection leaves their Archetype=Unknown; the vr_weapon_shell path's 'if Archetype!=Unknown' guard therefore skips the four new weapons themselves (the shell serves foreign weapons, not these).
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/engine/vr_weapon_helpers.zs:169-176 — A_CheckSpawnModel() (called by every new weapon's Spawn state) is an EMPTY STUB: it reads vr_spawn_models then only a comment '// Logic to swap ... if applicable'; the shotgun's inline vr_spawn_models block (weaponshotgun.zs:51) is likewise a stub.

ADVERSARIAL: survives=True finalVerdict=partial (conf high)
--- REFUTATION ---
I re-opened every cited file and traced the full chain; the original "partial (high)" verdict survives and is in fact reinforced. WORKING half: FVRWeaponResolver::Init() runs at startup (d_main.cpp:3770), and the render shell in hw_weapon.cpp (1684-1826) + p_pspr.cpp:677 genuinely reads ReadyWeapon->vr_weapon_data->Archetype and injects the archetype actor's model — the real universal-VR mechanism for foreign pk3 weapons that inherit recognized base classes (gated by vr_weapon_shell=true default). Three of the four new weapons DO get a 3D model, but NOT via the shell — via the class-agnostic sprite-to-model modeldef blocks Model VR_Rifle/Revolver/SMG (their Ready sprites RIFL A / REVL A / SMGS A are FrameIndex-mapped). PARTIAL/BROKEN halves I confirmed independently: (1) ResolveWeapons() at vr_weapon.cpp:39-49 has NO archetype branch for Rifle/SMG/Revolver/Flamethrower and the four actors inherit plain DoomWeapon (doomweapons.zs), so their own Archetype auto-resolves to Unknown and the shell skips them; no doomxr_weapons.json override exists to fix it. (2) The flamethrower actor uses sprite HBFT in every state (weaponflame.zs), but modeldef only maps FLMG — HBFT appears in zero modeldef entries — so the flamethrower's md3 (which exists on disk and in doomxr.pk3) never binds; flamethrower falls back to a flat sprite. (3) The advertised 'Spawn 3D Models instead of Sprites' toggle is dead: A_CheckSpawnModel() (vr_weapon_helpers.zs:169-176) reads vr_spawn_models then has only a comment body, and the shotgun inline block (weaponshotgun.zs:51) is likewise a stub. ADDITIONAL defect the first auditor missed: the WeaponReplacementHandler that actually hands these weapons to the player (vr_weapon_logic.zs) is a real auto-registering StaticEventHandler, but every distribution cvar defaults OFF — vr_chainsaw_flame_chance/vr_chaingun_rifle_chance/vr_chaingun_smg_chance/vr_weapon_revolver_chance = 0.0 and vr_start_with_rifle = false (CVARINFO:4,154-157) — so by default NONE of the four new weapons ever spawn or reach the player at all. Net: real render wiring exists and works for 3/4 weapons only when a user manually raises a menu slider; flamethrower model mapping is broken and the model-spawn toggle is a no-op. Partial, not working.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/src/playsim/vr_weapon.cpp:39-49 — ResolveWeapons() auto-detects ONLY Shotgun/SSG/Pistol/Chaingun/RocketLauncher/PlasmaRifle/BFG/Chainsaw/Fist; no Rifle/SMG/Revolver/Flamethrower branch, so the 4 new DoomWeapon-derived actors stay Archetype=Unknown and the shell (guarded 'if Archetype != Unknown' at hw_weapon.cpp:1686) never drives them
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/modeldef.txt:149-160 — Model VR_Flamethrower maps FrameIndex FLMG A, but weaponflame.zs uses sprite HBFT in every state; grep confirms HBFT is in zero modeldef entries, so the flamethrower md3 (present in doomxr.pk3) never binds — flat sprite fallback
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/engine/vr_weapon_helpers.zs:169-176 — A_CheckSpawnModel() (called by rifle RFP1/revolver RVP1/smg SMP1 Spawn states) reads vr_spawn_models then only has a comment '// Logic to swap...'; weaponshotgun.zs:51 inline block is likewise a stub — the advertised model-spawn toggle is a no-op
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/CVARINFO:4,154-157 — vr_start_with_rifle=false, vr_chainsaw_flame_chance/vr_chaingun_rifle_chance/vr_chaingun_smg_chance/vr_weapon_revolver_chance all =0.0, so WeaponReplacementHandler (vr_weapon_logic.zs) hands the player NONE of the four weapons by default — capability is entirely opt-in via menu sliders (menudef.txt:4341-4349)
  5. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_weapon.cpp:1703 + modeldef.txt:100-146 — the working path for rifle/revolver/smg is the class-agnostic modeldef sprite binding (RIFL A / REVL A / SMGS A mapped to md3), NOT the FVRWeaponResolver shell; both md3 sets exist in wadsrc and doomxr.pk3, so 3-of-4 render as 3D once enabled

####################################################################################################
CLAIM ID: weapons.3  |  CLUSTER: Weapons System (universal model set, dual-wield, assignment)
CLAIM: Proper weapon assignment in-game

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
Weapon assignment is real and wired through two independent paths, both traced end-to-end. (1) Automatic replacement/start-inventory: WeaponReplacementHandler is a StaticEventHandler (registers automatically), its CVar reads match declared CVARINFO entries, and it consumes CheckReplacementEvent/WorldThingSpawned to swap weapons and grant grenades. (2) User-driven archetype assignment: the Archetype Scanner menu is reachable from menudef, cycles archetypes via the real vr_weapon_set_archetype CCMD which persists to doomxr_weapons.json (loaded back on startup by FVRWeaponResolver::Init at d_main.cpp:3770), and — crucially — the assigned archetype is not a dead value: hw_weapon.cpp and p_pspr.cpp read it to pick the rendered model and drive animation state. The only caveat (not a defect in the claim) is that the replacement CVars default to 0.0, so random reassignment is off until tuned; that is a default, not broken wiring. I looked for but did not find any missing registration, missing consumer, or stub in this path.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/engine/vr_weapon_logic.zs:1-73 — WeaponReplacementHandler:StaticEventHandler (auto-registers, no mapinfo needed) implements CheckReplacement (Chainsaw->Flamethrower, Chaingun->Rifle/SMG, Pistol/Rifle->Revolver by CVar chance) and WorldThingSpawned (gives 3 HandGrenades, optional Pistol->Rifle swap via vr_start_with_rifle).
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/CVARINFO:154-157,4 — vr_chainsaw_flame_chance/vr_chaingun_rifle_chance/vr_chaingun_smg_chance/vr_weapon_revolver_chance and vr_start_with_rifle are declared and are the exact CVars the replacement handler reads (default 0.0/false = tuning off, but logic acts on them).
  3. E:/DoomXR-work/DOOM_FRESH/src/playsim/vr_weapon.cpp:218-245 — CCMD vr_weapon_set_archetype resolves the class, sets vr_weapon_data->Archetype, and calls SaveJSONOverrides() writing doomxr_weapons.json; LoadJSONOverrides (l.69) reloads it at startup.
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/menudef.txt:3076 + wadsrc/static/zscript/ui/vr_weapon_menu.zs:122-128 — the 'Open Weapon Archetype Scanner' menu is reachable and its CycleArchetype sends vr_weapon_set_archetype; GetVRWeaponArchetype (native, vr_weapon.cpp:247) reads the value back for display.
  5. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_weapon.cpp:1703 + p_pspr.cpp:677 — the archetype the user assigns is actually CONSUMED: GetActorClassForArchetype drives the rendered 3D model and animation state, so assignment changes in-game behavior, not just a stored value.

ADVERSARIAL: survives=False finalVerdict=partial (conf high)
--- REFUTATION ---
Split in two. (A) AUTOMATIC replacement (WeaponReplacementHandler: Chainsaw->Flamethrower, Chaingun->Rifle/SMG, free starting grenades, vr_start_with_rifle) is DEAD: it is a StaticEventHandler NOT in common.txt:8 AddEventHandlers, and there are zero .Register() calls tree-wide + pk3, so its overrides never fire; the vr_*_chance cvars are read only inside the dead handler. (B) MANUAL archetype assignment via the Scanner menu -> vr_weapon_set_archetype CCMD -> doomxr_weapons.json -> GetActorClassForArchetype drives the live 3D model (hw_weapon.cpp:1703/1820) + animation (p_pspr.cpp:677); CCMDs need no handler, so this half is fully working. Overall PARTIAL, not working.
 --- ADVERSARY EVIDENCE ---
  1. vr_weapon_logic.zs:1 — WeaponReplacementHandler:StaticEventHandler, absent from common.txt:8 AddEventHandlers, zero .Register() anywhere -> auto-replacement never fires
  2. src/playsim/vr_weapon.cpp:218,243-244 — CCMD vr_weapon_set_archetype sets Archetype + SaveJSONOverrides; manual path is handler-independent
  3. hw_weapon.cpp:1703/1820 + p_pspr.cpp:677 — assigned archetype IS consumed by live render+animation (gated vr_weapon_shell=true)

####################################################################################################
CLAIM ID: render.3  |  CLUSTER: World Render Modes, Fog & Enhanced FX
CLAIM: Enhanced FX and explosions with a master toggle for pre-set effect levels (light / med / heavy)

INVESTIGATOR VERDICT: working (conf high)

--- INVESTIGATOR REASONING ---
This is the one claim wired end-to-end. vr_visual_fidelity is exposed as a 4-level menu option and is READ by branching logic on multiple LIVE actor states: the rocket launcher escalates its trail FX (weaponrlaunch.zs:74-79) and the flamethrower scales particle count as 1+fidelity (weaponflame.zs:42-47), plus voxel-assembly and ROM-load effects. These are actor-state actions executed during normal play, and the branches produce visibly different FX per level — a real light/med/heavy master toggle for enhanced FX/explosions. A complementary intensity axis (gitd_death_intensity / hf_impact_intensity) is consumed by the death-FX machine whose handler GITD_DeathFXHandler IS registered (zmapinfo). I looked for but found no evidence these consumers are stubbed or short-circuited; the CVar reads feed directly into spawn counts/branches. Only caveat: 'master' is loosely worded — it is really two cvar axes (vr_visual_fidelity for weapon/effect tiers, gitd_death_intensity/hf_impact_intensity for death FX) rather than one unified switch — but the light/med/heavy preset-level behavior described is present and functional.

--- INVESTIGATOR EVIDENCE ---
  1. E:/_gitd35_repo/menudef:4321-4337 — a 4-level VisualFidelity OptionValue (Low/Medium/High/Ultra) is bound to the vr_visual_fidelity CVar via Option 'System Performance / Effects' in a real OptionMenu
  2. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/doom/weaponrlaunch.zs:74-79 — the Rocket's live Spawn state reads vr_visual_fidelity and BRANCHES: fidelity>0 spawns FlameSmoke trail, fidelity>1 additionally spawns FlameParticle (tiered explosion/projectile-FX escalation)
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/doom/weaponflame.zs:42-47 — the flamethrower's live Hold-fire state computes pCount = 1 + fidelity and loops that many FlameParticle spawns (1/2/3/4), directly scaling FX density by the toggle
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/effects/vr_voxel_assembly.zs:47 and wadsrc/static/zscript/ui/vr_rom_load.zs:20 — two more independent live consumers read vr_visual_fidelity.GetInt() and act on it, confirming a broadly-consumed toggle, not a single stub
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd3_deathfx.zs:343,700-792,1440 — the death/impact FX machine additionally scales output by gitd_death_intensity and reads hf_impact_intensity, a second real intensity-scaling axis on the registered GITD_DeathFXHandler

ADVERSARIAL: survives=False finalVerdict=partial (conf high)
--- REFUTATION ---
The original verdict "working (high)" does NOT survive. The claim's headline — a light/med/heavy MASTER TOGGLE driven by vr_visual_fidelity — is broken, because the CVar it depends on is DECLARED NOWHERE. I searched both repos, the C++ engine source, the source CVARINFO, and the freshly-baked build/Release/doomxr.pk3 CVARINFO (rebuilt 2026-07-02): vr_visual_fidelity appears only as a menu Option/OptionValue and as 4 ZScript reads — never as a `server/user int` declaration. I traced the runtime path: GetCVar() (c_cvars.cpp:1457-1462) returns nullptr for an unknown cvar; the ZScript CVar.GetInt native (vmnatives.cpp:909-914) does PARAM_SELF_STRUCT_PROLOGUE(FBaseCVar) on that null self → a VM abort ("read from address zero"). All four consumers use the UNGUARDED form `CVar.GetCVar("vr_visual_fidelity", ...).GetInt()` with no null check (weaponrlaunch.zs:74, weaponflame.zs:42, vr_voxel_assembly.zs:47, vr_rom_load.zs:20), so each one crashes/aborts the moment it executes during normal play (firing the rocket, firing the flamethrower, voxel assembly, ROM load). Worse, the menu Option itself is dead: optionmenuitems.zs:415 does mCVar = CVar.FindCVar(mAction) and guards every read/write with `mCVar != null` (line 425), and neither FindCVar nor the menu auto-creates the cvar — so the "System Performance / Effects" row shows a frozen value and changing it does nothing. The branching logic the original cited is REAL as source text, but it is wired to a phantom cvar, so it does not run — it faults. What IS genuinely working is the SEPARATE intensity axis: gitd_death_intensity (float=1.0) and hf_impact_intensity (int=1) ARE declared in CVARINFO (source and baked pk3) and ARE consumed with the SAFE guarded pattern (GFloat helper at gitd3_deathfx.zs:300 null-checks; the hf_impact read at :1440 uses `c ? c.GetInt() : 1`) inside GITD_DeathFXHandler, which IS registered in zmapinfo. So the death-FX intensity scaling functions; the vr_visual_fidelity weapon/effect-tier "master toggle" does not. Half real, half broken → partial, not working. Note the tell-tale contrast: the death-FX code null-guards every cvar read, while all four fidelity reads do not — and only the fidelity cvar was left undeclared.
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/CVARINFO and build/Release/doomxr.pk3!CVARINFO — grep for 'fidelity'/'vr_visual' returns ZERO hits in both source and baked pk3; the cvar is declared nowhere (C++ src/ also clean)
  2. E:/DoomXR-work/DOOM_FRESH/src/common/console/c_cvars.cpp:1457-1462 — GetCVar() returns nullptr for an unknown cvar name; combined with vmnatives.cpp:909-914 GetInt's PARAM_SELF_STRUCT_PROLOGUE(FBaseCVar) on null self → VM abort, so the unguarded fidelity reads fault at runtime
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/actors/doom/weaponflame.zs:42 (and weaponrlaunch.zs:74, vr_voxel_assembly.zs:47, vr_rom_load.zs:20) — all four use CVar.GetCVar("vr_visual_fidelity",...).GetInt() with NO null guard, dereferencing a cvar that does not exist
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/engine/ui/menu/optionmenuitems.zs:415,425 — the Option item does mCVar=CVar.FindCVar(mAction) then guards with `mCVar != null`; FindCVar does not auto-create, so the menu row is a dead no-op for the undeclared cvar
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd3_deathfx.zs:300,1440 — the SEPARATE intensity axis is the genuinely-working part: GFloat()/FindCVar null-guarded reads of gitd_death_intensity & hf_impact_intensity, both DECLARED in CVARINFO (:110,:107), inside the zmapinfo-registered GITD_DeathFXHandler

####################################################################################################
CLAIM ID: render.1  |  CLUSTER: World Render Modes, Fog & Enhanced FX
CLAIM: 12+ different render modes for the game world (TRON, Wireframe, Cyberspace / System Shock, etc.)

INVESTIGATOR VERDICT: declared_unwired (conf high)

--- INVESTIGATOR REASONING ---
Two disconnected pipelines exist and neither closes the loop for world render MODES. (1) main.fp's applyVisualRegime() is genuinely called on the live path (main.fp:2011) with real per-regime logic, but it gates on the StreamData uniform u_vr_visual_regime, which C++ only initializes to 0 (hw_renderstate.h:392) and never updates. The ZScript GITD_ShaderBridge that should feed it calls Shader.SetUniformInt/Float/Vec3 — methods that do not exist anywhere (native Shader struct only has deprecated PPShader-wrapping SetUniform1f/1i taking a PlayerInfo first arg), and the bridge class is not in any addeventhandlers list, so its UiTick never fires. (2) The real CVar vr_visual_regime IS wired into the ViewpointUBO as uVisualRegime (hw_drawinfo.cpp:196, pushed in gles/gl/vulkan), but I could find no fragment shader that reads uVisualRegime — the fp only reads the never-set u_vr_visual_regime. Net: the regime gate is always 0, applyVisualRegime always early-returns, the world is never re-rendered in any mode. The count is also wrong: 7 branches, not 12+. The one genuinely-wired arcade world effect is uAffineWarp (PS1 vertex wobble, main.vp:122) fed by vr_affine_warp, but that is a separate boolean, not a named render mode.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/shaders/glsl/main.fp:1771-1898 — applyVisualRegime implements only 7 regime branches (u_vr_visual_regime == 1,2,3,4,5,7,9; gaps at 6 and 8, 0=off), NOT 12+
  2. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_renderstate.h:392 — the StreamData uniform u_vr_visual_regime (read by the shader) is only ever WRITTEN to 0; grep found no other assignment in all of src/, so the gate is permanently off
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/doombase.zs:923-950 — native Shader struct defines only SetEnabled/SetUniform1f/2f/3f/1i (deprecated PPShader wrappers taking a PlayerInfo); SetUniformInt/SetUniformFloat/SetUniformVec3 called by the bridge have ZERO definitions engine-wide
  4. E:/_gitd35_repo/zmapinfo:3 — addeventhandlers list does NOT contain GITD_ShaderBridge (also absent from baked doomxr.pk3 mapinfo/common.txt), so the UiTick() that would push u_vr_visual_regime never runs
  5. E:/DoomXR-work/DOOM_FRESH/src/rendering/hwrenderer/scene/hw_drawinfo.cpp:196 — real CVar vr_visual_regime IS copied into VPUniforms.mVisualRegime/uVisualRegime (ViewpointUBO), but grep shows NO fragment shader reads uVisualRegime; only main.vp:122 reads the separate uAffineWarp, so the regime CVar reaches the GPU but nothing consumes it

ADVERSARIAL: survives=True finalVerdict=declared_unwired (conf high)
--- REFUTATION ---
UPHELD: 0 of 12 world render modes run. Two near-identically-named vars never connect: (1) CVar vr_visual_regime -> ViewpointUBO uVisualRegime reaches the GPU but NO .fp/.vp reads it (producer, no consumer); (2) StreamData u_vr_visual_regime is what main.fp:2011 applyVisualRegime actually gates on, but its only write in all of src/ is `= 0` (hw_renderstate.h:392) (consumer, no producer). applyVisualRegime implements 7 branches (1,2,3,4,5,7,9; System Shock/Tron/Blueprint labeled) not 12, all unreachable. FIX PATH (cheap): point main.fp at the already-fed uVisualRegime instead of the dead u_vr_visual_regime -> loose-override shader edit, no recompile. Sub-params (react/bubble/param) are StreamData-only and still dead even then.
 --- ADVERSARY EVIDENCE ---
  1. main.fp:1756,2011 — applyVisualRegime called live but early-returns on u_vr_visual_regime<=0
  2. hw_renderstate.h:392 — the ONLY write to u_vr_visual_regime is =0; no producer
  3. hw_drawinfo.cpp:196 — vr_visual_regime CVar IS copied to uVisualRegime (ViewpointUBO) but grep shows no shader reads uVisualRegime

####################################################################################################
CLAIM ID: render.2  |  CLUSTER: World Render Modes, Fog & Enhanced FX
CLAIM: Shader-intensive arcade-themed fog and world effects

INVESTIGATOR VERDICT: declared_unwired (conf high)

--- INVESTIGATOR REASONING ---
The fog modes are real in intent (4 arcade modes) and applyOmniFog is genuinely wired into the live fragment path (main.fp:1990, normal texture branch). But every branch is gated on u_gitd_fog_mode, a StreamData UBO field C++ only initializes to 0 and never updates (hw_renderstate.h:385). Because u_gitd_fog_mode <= 0 the function immediately returns stock applyFog. The intended feeder is the same broken GITD_ShaderBridge (phantom Shader.SetUniform* methods + unregistered handler). Unlike the regime CVar, gitd_fog_mode has NO C++ CVar and NO ViewpointUBO path at all — grep of the entire engine src/ finds it only as a bare StreamData struct field, so there is not even a half-connected alternate route. The shader logic is real and on a live path, but it can never receive a non-zero mode, so no arcade fog ever renders.

--- INVESTIGATOR EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/shaders/glsl/main.fp:1713-1752 — applyOmniFog implements 4 real arcade fog modes (Ground-Mist, Spectral-Silhouette rim, Bit-Crush quantize, Vortex noise-swirl) and IS called on the live path at main.fp:1990
  2. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_renderstate.h:385 — u_gitd_fog_mode (the applyOmniFog gate) is only ever assigned 0; no other write exists in src/, so applyOmniFog always hits its early-return at main.fp:1715 and falls back to stock exp2 fog
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd_shaderbridge.zs:56-62 — the only code that would set u_gitd_fog_mode/density calls non-existent Shader.SetUniformInt/Float, inside a class never registered as an event handler
  4. E:/_gitd35_repo/cvarinfo:206-207 — gitd_fog_mode/gitd_fog_density exist only as mod USER cvars; grep of src/ shows no C++ reads them (unlike vr_visual_regime), so there is not even a partial C++ bridge for fog
  5. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/shaders/glsl/main.fp:1722 — Light-Link fog-color-from-room-glow logic exists but is unreachable because u_gitd_fog_lightlink is likewise a never-fed StreamData uniform

ADVERSARIAL: survives=True finalVerdict=declared_unwired (conf high)
--- REFUTATION ---
The original declared_unwired verdict survives; if anything it understated the breakage (the bridge also targets the wrong uniform system).
 --- ADVERSARY EVIDENCE ---
  1. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/shaders/glsl/main.fp:1715 — applyOmniFog early-returns stock applyFog whenever u_gitd_fog_mode <= 0, gating all 4 arcade fog modes behind that uniform
  2. E:/DoomXR-work/DOOM_FRESH/src/common/rendering/hwrenderer/data/hw_renderstate.h:385 — the ONLY write to mStreamData.u_gitd_fog_mode in all of src/ is `= 0` (init); no per-frame C++ feeder exists despite the 'B2 feeds live values' comment at line 384
  3. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/doombase.zs:923-950 — native struct Shader defines only SetEnabled/SetUniform1f/2f/3f/1i; it has NO SetUniformInt/SetUniformFloat/SetUniformVec3, so the bridge's calls are compile errors
  4. E:/DoomXR-work/DOOM_FRESH/wadsrc/static/zscript/radiance/gitd_shaderbridge.zs:56-62 — the only would-be feeder calls non-existent Shader.SetUniformInt/Float; also targets PPShader namespace, not the main-scene StreamData UBO that main.fp reads (double mismatch)
  5. E:/DoomXR-work/DOOM_FRESH/build/Release/doomxr.pk3 (zscript/doombase.zs:923 + zscript/radiance/gitd_shaderbridge.zs:56) — the SHIPPED build carries the same broken pairing: Shader struct lacks the methods, bridge still calls them; grep of entire pk3 finds zero definition of SetUniformInt/Float/Vec3